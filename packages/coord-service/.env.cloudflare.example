# Copy to .env.cloudflare (git-ignored, see repo root .gitignore) and fill in real values.
# Never commit the real file. Scoped API token only — see AGENTS.md for the exact
# permissions to grant (Workers Scripts:Edit, D1:Edit, scoped to one account).
export CLOUDFLARE_API_TOKEN=""
export CLOUDFLARE_ACCOUNT_ID=""
