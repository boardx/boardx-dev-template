# Sprint p18/03 — Wave 2 依赖项：研究报告双模板

- **所属阶段**: Phase p18 (ava-ai-realization)
- **创建于**: 2026-07-07（随 F05 开工时按需 scaffold；见 issue #421）

## 本 sprint 目标
Wave 2 依赖项：F04（Deep Research 真实生成）落地后，报告面板按研究类型渲染双模板。

## 领取的 feature(引用自阶段权威清单,按 id)
- F05 (P3, deep-research) — 研究报告双模板（market / user-research）

> 实际工作集见同目录 `active-features.json`(脚本派生,只读,勿手改)。
> 修改功能归属:改阶段 `feature_list.json` 里对应 feature 的 `sprint` 字段,再重跑
> `pnpm harness new-sprint`(或 refresh)重新派生。

## 完成标准
- 上述每个 feature 经 `pnpm harness verify --sprint p18/03` 门控为 `passing`。
- `session-handoff.md` 与 `progress.md` 已更新。
