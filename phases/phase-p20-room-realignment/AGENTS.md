# AGENTS.md — Phase p20 (Room Realignment) 局部指令

> 阶段级 scoped 指令,补充根 AGENTS.md。只写本阶段特有的约束。

## 本阶段焦点
Room 对齐重构：修复 Room 相对原型与旧代码设计意图的结构性 gap——房间详情壳+tab 导航、创建时可见性、房间级文件库、收藏、删除契约、权限口径统一、Room Survey 入口、完整邀请流、下线 legacy 单画布、Room AI 上下文字段

## 权威来源
- 功能清单:本目录 `feature_list.json`(本阶段唯一权威)。
- 进度:本目录 `progress.md`。

## 规则继承
根 `AGENTS.md` 的所有硬约束在此继续生效(尤其"完成定义"与"干净收尾")。
