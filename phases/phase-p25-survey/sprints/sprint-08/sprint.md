# Sprint p25/08 — 同步 boardx-survey 未提交首页与模板导航设计

- **所属阶段**: Phase p25 (survey)
- **创建于**: 2026-07-14 09:42:35

## 本 sprint 目标
同步 boardx-survey 未提交首页与模板导航设计

## 领取的 feature(引用自阶段权威清单,按 id)
- F08 (P1, survey) — 同步 Survey 未提交首页与模板导航设计

> 实际工作集见同目录 `active-features.json`(脚本派生,只读,勿手改)。
> 修改功能归属:改阶段 `feature_list.json` 里对应 feature 的 `sprint` 字段,再重跑
> `pnpm harness new-sprint`(或 refresh)重新派生。

## 完成标准
- 上述每个 feature 经 `pnpm harness verify --sprint p25/08` 门控为 `passing`。
- `session-handoff.md` 与 `progress.md` 已更新。
