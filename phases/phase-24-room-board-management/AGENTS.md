# AGENTS.md — Phase 24 (room-board-management) 局部指令

> 阶段级 scoped 指令,补充根 AGENTS.md。只写本阶段特有的约束。

## 本阶段焦点
Room 内 Board 列表管理:缩略图卡片网格 + Grid/List 切换 + 新建带多标签(Dialog) + 卡片更多操作菜单(删除/重命名/编辑标签/上传·移除封面/移动) + 按 tag 过滤。对齐 oldcode uc-board-001/003/005/008 全套 + 净新增多标签过滤;多标签需 boards.tags text[] schema。

## 权威来源
- 功能清单:本目录 `feature_list.json`(本阶段唯一权威)。
- 进度:本目录 `progress.md`。

## 规则继承
根 `AGENTS.md` 的所有硬约束在此继续生效(尤其"完成定义"与"干净收尾")。
