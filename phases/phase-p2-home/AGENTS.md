# AGENTS.md — Phase p2 (Home 工作台 (P2)) 局部指令

> 阶段级 scoped 指令,补充根 AGENTS.md。只写本阶段特有的约束。

## 本阶段焦点
首页工作台：欢迎区(用户/团队名)/Recent/Agent 搜索骨架/推荐功能入口/Onboarding。AVA 相关动作 blocked-on p9

## 权威来源
- 功能清单:本目录 `feature_list.json`(本阶段唯一权威)。
- 进度:本目录 `progress.md`。

## 规则继承
根 `AGENTS.md` 的所有硬约束在此继续生效(尤其"完成定义"与"干净收尾")。
