# Room Chat 用例

本目录描述用户在房间 Chat 页签中使用 AVA 房间聊天的场景。

覆盖范围：
- 新建房间聊天。
- 查看、分组并打开房间聊天线程。
- 在房间聊天中发送消息。
- 删除自己可删除的聊天线程。

角色边界：
- Room owner/admin/member 都可以使用自己已加入房间的 Chat 页签。
- 非房间成员会被房间访问检查拦截。
- 删除菜单只在当前页面判断为当前用户相关的线程卡片上出现。

覆盖审计提示集中写入各具体用例的“业务规则”字段。
