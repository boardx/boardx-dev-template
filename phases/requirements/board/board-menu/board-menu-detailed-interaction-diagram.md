# Board Menu 详细交互图

本图按 Board Menu 模块当前 Use Case 的主流程展开，重点表达每个界面能看到什么、能操作什么、操作后出现什么状态。若某个 UC 继续细化，应该同步更新该 UC 的主流程和本图。

## 模块交互展开

该部分保留当前模块已有交互图，用于表达模块内主要页面区域之间的流转。

```mermaid
flowchart TD
  Menu["Board Menu"] --> Select["选择工具"]
  Menu --> Pan["平移工具"]
  Menu --> Sticky["便利贴"]
  Menu --> Draw["手绘"]
  Menu --> Text["文本"]
  Menu --> Connector["连接线"]
  Menu --> Shape["形状"]
  Menu --> Resources["资源"]
  Menu --> Template["模板"]

  Select --> SelectionMode["进入选择、多选、移动和编辑状态"]
  Pan --> PanMode["拖动画布移动视角"]
  Sticky --> StickyCreate["点击画布创建便利贴"]
  Text --> TextCreate["点击画布创建文本"]
  Shape --> ShapePicker["选择圆形、三角形、菱形、圆角矩形、矩形或六边形"]
  ShapePicker --> ShapeCreate["拖拽创建形状"]
  Connector --> ConnectorCreate["拖拽创建自由连接线或对象连接"]
  Draw --> DrawMode["绘制笔迹"]
  DrawMode --> Eraser["切换橡皮擦擦除笔迹"]
  Resources --> ResourcePanel["打开图片、图标、标签资源面板"]
  ResourcePanel --> InsertResource["插入资源到画布"]
  Template --> TemplatePanel["打开模板面板"]
  TemplatePanel --> InsertTemplate["插入模板内容到画布"]
```

## Use Case 主流程展开

```mermaid
flowchart TD
  subgraph D1G["使用 Board Menu"]
    D1Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D1Entry["可见入口/区域：用户进入可编辑白板后，在画布边缘看到 Board Menu 工具栏。；当前 Boar<br/>d Menu 直接渲染的入口包括选择、平移、便利贴、手绘、文本、连接线、形状、资源和<br/>模板。；当前 Board Menu 未直接渲染文件上传、链接、图表或 AI 助手按钮<br/>；这些能力只有在其它可见入口明确展示时，才属于用户可操作能力。"]
    D1Start --> D1Entry
    D1S1["1. 用户进入可编辑白板，系统在画布边缘显示 Board Menu 工具栏，并按当前角色<br/>和屏幕尺寸展示可用工具。"]
    D1Entry --> D1S1
    D1S2["2. 用户可以看到选择、平移、便利贴、手绘、文本、连接线、形状、资源和模板入口；不可用入<br/>口被隐藏、禁用或不展示。"]
    D1S1 --> D1S2
    D1S3["3. 用户悬停或聚焦某个工具时，系统显示工具名称或状态提示，并保持画布内容不变。"]
    D1S2 --> D1S3
    D1S4["4. 用户点击工具时，系统高亮当前工具，切换光标、创建模式或打开对应面板。"]
    D1S3 --> D1S4
    D1S5["5. 用户点击资源入口时，系统打开资源面板；用户可看到搜索框以及图片、图标等当前资源标签<br/>。"]
    D1S4 --> D1S5
    D1S6["6. 用户点击模板入口时，系统打开模板面板，并展示可插入到当前 Board 的模板内容。"]
    D1S5 --> D1S6
    D1S7["7. 用户把支持拖拽或放置的工具用于画布时，系统显示放置结果、选中框、Widget Me<br/>nu、加载状态或失败提示。"]
    D1S6 --> D1S7
    D1S8["8. 用户切换到选择工具、按 Esc 或点击其它工具时，系统退出当前创建模式并更新工具高<br/>亮。"]
    D1S7 --> D1S8
    D1S9["9. 当用户没有编辑权限时，系统只保留查看、平移、缩放等不改变内容的入口。"]
    D1S8 --> D1S9
    D1Alt{"备选：1. 用户按 Esc、点击选择工具或切换到其他工具时，系统退出当前创建模式。；2. 工具<br/>入口不可用时，系统隐藏、禁用或提示当前无法使用。；3. Visitor 和 public<br/> visitor 不显示会造成内容变更的完整创建工具。"}
    D1S9 --> D1Alt
    D1Err{"异常：1. 创建、上传或生成失败时，系统保留已有内容并显示失败反馈。；2. 权限在操作中变化时<br/>，系统停止写入并提示无权限。；3. 用户拖拽到不可用区域时，系统不创建内容或回到可放置状<br/>态。"}
    D1S9 --> D1Err
    D1Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D1S9 --> D1Perm
  end
  subgraph D2G["创建便利贴"]
    D2Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D2Entry["可见入口/区域：用户进入可编辑白板后，在画布边缘看到 Board Menu 工具栏。；当前 Boar<br/>d Menu 直接渲染的入口包括选择、平移、便利贴、手绘、文本、连接线、形状、资源和<br/>模板。；当前 Board Menu 未直接渲染文件上传、链接、图表或 AI 助手按钮<br/>；这些能力只有在其它可见入口明确展示时，才属于用户可操作能力。"]
    D2Start --> D2Entry
    D2S1["1. 用户在 Board Menu 中看到便利贴入口和展开按钮。"]
    D2Entry --> D2S1
    D2S2["2. 用户点击便利贴入口，系统高亮便利贴工具，把光标切换为便利贴样式，并进入便利贴创建模<br/>式；再次点击当前便利贴入口时，系统回到选择状态。"]
    D2S1 --> D2S2
    D2S3["3. 用户打开便利贴展开面板时，系统展示矩形、方形、圆形样式和颜色选择。"]
    D2S2 --> D2S3
    D2S4["4. 用户选择样式或颜色后，系统更新当前便利贴工具状态，并继续保持便利贴创建模式。"]
    D2S3 --> D2S4
    D2S5["5. 用户在画布目标位置点击，系统按默认尺寸创建便利贴。"]
    D2S4 --> D2S5
    D2S6["6. 系统创建便利贴后选中新便利贴，显示选中框和可用的 Widget Menu。"]
    D2S5 --> D2S6
    D2S7["7. 用户输入文字时，系统在便利贴内显示文字，并根据便利贴尺寸排版。"]
    D2S6 --> D2S7
    D2S8["8. 用户可以继续通过 Widget Menu 调整当前可见的背景色、文字样式、边框、大<br/>小、形态或锁定状态。"]
    D2S7 --> D2S8
    D2S9["9. 用户切换工具或按 Esc 时，系统退出便利贴创建模式。"]
    D2S8 --> D2S9
    D2Alt{"备选：1. 用户按 Esc、点击选择工具或切换到其他工具时，系统退出当前创建模式。；2. 工具<br/>入口不可用时，系统隐藏、禁用或提示当前无法使用。；3. Visitor 和 public<br/> visitor 不显示会造成内容变更的完整创建工具。"}
    D2S9 --> D2Alt
    D2Err{"异常：1. 创建、上传或生成失败时，系统保留已有内容并显示失败反馈。；2. 权限在操作中变化时<br/>，系统停止写入并提示无权限。；3. 用户拖拽到不可用区域时，系统不创建内容或回到可放置状<br/>态。"}
    D2S9 --> D2Err
    D2Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D2S9 --> D2Perm
  end
  subgraph D3G["创建文本"]
    D3Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D3Entry["可见入口/区域：用户进入可编辑白板后，在画布边缘看到 Board Menu 工具栏。；当前 Boar<br/>d Menu 直接渲染的入口包括选择、平移、便利贴、手绘、文本、连接线、形状、资源和<br/>模板。；当前 Board Menu 未直接渲染文件上传、链接、图表或 AI 助手按钮<br/>；这些能力只有在其它可见入口明确展示时，才属于用户可操作能力。"]
    D3Start --> D3Entry
    D3S1["1. 用户在 Board Menu 中看到文本或标题入口。"]
    D3Entry --> D3S1
    D3S2["2. 用户点击文本入口，系统高亮文本工具，并把光标切换为文本输入状态。"]
    D3S1 --> D3S2
    D3S3["3. 用户在画布点击目标位置，系统创建默认宽度的文本对象，并进入可输入状态。"]
    D3S2 --> D3S3
    D3S4["4. 用户输入文字，系统在画布上实时显示文本内容。"]
    D3S3 --> D3S4
    D3S5["5. 用户选中文本后拖拽文本边界或控制点时，系统按当前组件能力调整文本框大小，并重新排布<br/>文本。"]
    D3S4 --> D3S5
    D3S6["6. 用户选中文本后，系统显示文本相关 Widget Menu，包括字号、字体、字重、对<br/>齐、文字色、背景色等当前可见入口。"]
    D3S5 --> D3S6
    D3S7["7. 用户点击画布空白处或切换工具时，系统结束文本编辑并保留文本对象。"]
    D3S6 --> D3S7
    D3S8["8. 如果用户没有输入内容就退出，系统按当前界面规则保留空文本或移除空对象。"]
    D3S7 --> D3S8
    D3Alt{"备选：1. 用户按 Esc、点击选择工具或切换到其他工具时，系统退出当前创建模式。；2. 工具<br/>入口不可用时，系统隐藏、禁用或提示当前无法使用。；3. Visitor 和 public<br/> visitor 不显示会造成内容变更的完整创建工具。"}
    D3S8 --> D3Alt
    D3Err{"异常：1. 创建、上传或生成失败时，系统保留已有内容并显示失败反馈。；2. 权限在操作中变化时<br/>，系统停止写入并提示无权限。；3. 用户拖拽到不可用区域时，系统不创建内容或回到可放置状<br/>态。"}
    D3S8 --> D3Err
    D3Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D3S8 --> D3Perm
  end
  subgraph D4G["创建图形"]
    D4Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D4Entry["可见入口/区域：用户进入可编辑白板后，在画布边缘看到 Board Menu 工具栏。；当前 Boar<br/>d Menu 直接渲染的入口包括选择、平移、便利贴、手绘、文本、连接线、形状、资源和<br/>模板。；当前 Board Menu 未直接渲染文件上传、链接、图表或 AI 助手按钮<br/>；这些能力只有在其它可见入口明确展示时，才属于用户可操作能力。"]
    D4Start --> D4Entry
    D4S1["1. 用户在 Board Menu 中看到形状入口及可用形状下拉入口。"]
    D4Entry --> D4S1
    D4S2["2. 用户打开形状选择，系统展示圆形、三角形、菱形、圆角矩形、矩形和六边形等形状。"]
    D4S1 --> D4S2
    D4S3["3. 用户选择一种形状，系统把该形状设为当前创建工具，并在工具按钮上显示对应状态。"]
    D4S2 --> D4S3
    D4S4["4. 用户在画布按下并拖拽，系统显示正在创建的形状；拖拽范围决定形状宽高。"]
    D4S3 --> D4S4
    D4S5["5. 用户松开鼠标或触控点后，系统创建形状；如果尺寸过小，系统取消创建并回到选择状态。"]
    D4S4 --> D4S5
    D4S6["6. 系统创建形状后显示选中框、缩放控制点和可编辑区域。"]
    D4S5 --> D4S6
    D4S7["7. 用户输入形状文字时，系统在形状内部显示文字并按形状尺寸排版。"]
    D4S6 --> D4S7
    D4S8["8. 用户选中形状后，系统显示背景色、边框、文字样式、连接线、锁定和删除等当前可见入口。"]
    D4S7 --> D4S8
    D4S9["9. 用户切换工具或按 Esc 时，系统退出形状创建模式。"]
    D4S8 --> D4S9
    D4Alt{"备选：1. 用户按 Esc、点击选择工具或切换到其他工具时，系统退出当前创建模式。；2. 工具<br/>入口不可用时，系统隐藏、禁用或提示当前无法使用。；3. Visitor 和 public<br/> visitor 不显示会造成内容变更的完整创建工具。"}
    D4S9 --> D4Alt
    D4Err{"异常：1. 创建、上传或生成失败时，系统保留已有内容并显示失败反馈。；2. 权限在操作中变化时<br/>，系统停止写入并提示无权限。；3. 用户拖拽到不可用区域时，系统不创建内容或回到可放置状<br/>态。"}
    D4S9 --> D4Err
    D4Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D4S9 --> D4Perm
  end
```

```mermaid
flowchart TD
  subgraph D5G["创建连接线"]
    D5Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D5Entry["可见入口/区域：用户进入可编辑白板后，在画布边缘看到 Board Menu 工具栏。；当前 Boar<br/>d Menu 直接渲染的入口包括选择、平移、便利贴、手绘、文本、连接线、形状、资源和<br/>模板。；当前 Board Menu 未直接渲染文件上传、链接、图表或 AI 助手按钮<br/>；这些能力只有在其它可见入口明确展示时，才属于用户可操作能力。"]
    D5Start --> D5Entry
    D5S1["1. 用户在 Board Menu 中看到连接线或箭头连接线入口。"]
    D5Entry --> D5S1
    D5S2["2. 用户点击连接线入口，系统高亮连接线工具，并进入连接线创建模式。"]
    D5S1 --> D5S2
    D5S3["3. 用户从画布空白处拖拽到目标位置，系统显示起点、终点和路径预览。"]
    D5S2 --> D5S3
    D5S4["4. 用户从对象锚点拖拽到另一个对象时，系统高亮可连接目标，并显示连接反馈。"]
    D5S3 --> D5S4
    D5S5["5. 用户松开鼠标或触控点后，系统创建连接线，并保留自由端点或对象连接关系。"]
    D5S4 --> D5S5
    D5S6["6. 用户选中连接线时，系统显示端点控制点和连接线 Widget Menu。"]
    D5S5 --> D5S6
    D5S7["7. 用户可以调整线宽、连接线形态、端点箭头等当前可见样式；其它样式入口只有实际显示时才<br/>可操作。"]
    D5S6 --> D5S7
    D5S8["8. 用户切换工具、按 Esc 或点击选择工具时，系统退出连接线创建模式。"]
    D5S7 --> D5S8
    D5Alt{"备选：1. 用户按 Esc、点击选择工具或切换到其他工具时，系统退出当前创建模式。；2. 工具<br/>入口不可用时，系统隐藏、禁用或提示当前无法使用。；3. Visitor 和 public<br/> visitor 不显示会造成内容变更的完整创建工具。"}
    D5S8 --> D5Alt
    D5Err{"异常：1. 创建、上传或生成失败时，系统保留已有内容并显示失败反馈。；2. 权限在操作中变化时<br/>，系统停止写入并提示无权限。；3. 用户拖拽到不可用区域时，系统不创建内容或回到可放置状<br/>态。"}
    D5S8 --> D5Err
    D5Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D5S8 --> D5Perm
  end
  subgraph D6G["在画布上手绘"]
    D6Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D6Entry["可见入口/区域：用户进入可编辑白板后，在画布边缘看到 Board Menu 工具栏。；当前 Boar<br/>d Menu 直接渲染的入口包括选择、平移、便利贴、手绘、文本、连接线、形状、资源和<br/>模板。；当前 Board Menu 未直接渲染文件上传、链接、图表或 AI 助手按钮<br/>；这些能力只有在其它可见入口明确展示时，才属于用户可操作能力。"]
    D6Start --> D6Entry
    D6S1["1. 用户在 Board Menu 中看到手绘或画笔入口。"]
    D6Entry --> D6S1
    D6S2["2. 用户点击手绘入口，系统高亮手绘工具，并显示画笔光标或绘制状态。"]
    D6S1 --> D6S2
    D6S3["3. 系统自动打开手绘设置面板，用户可以选择 4px、8px、12px 笔宽和当前可见颜<br/>色，也可以切换到橡皮擦。"]
    D6S2 --> D6S3
    D6S4["4. 用户在画布按住鼠标、触控板或触控笔并拖动，系统实时显示笔迹。"]
    D6S3 --> D6S4
    D6S5["5. 用户松开后，系统把笔迹保存为可选中的绘制对象。"]
    D6S4 --> D6S5
    D6S6["6. 用户选中笔迹时，系统显示可用的线条样式、锁定、删除或 AI 等入口。"]
    D6S5 --> D6S6
    D6S7["7. 用户切换到选择、平移、橡皮或其它工具时，系统退出手绘模式。"]
    D6S6 --> D6S7
    D6S8["8. 如果绘制失败或权限变化，系统保留已有内容并显示失败或无权限提示。"]
    D6S7 --> D6S8
    D6Alt{"备选：1. 用户按 Esc、点击选择工具或切换到其他工具时，系统退出当前创建模式。；2. 工具<br/>入口不可用时，系统隐藏、禁用或提示当前无法使用。；3. Visitor 和 public<br/> visitor 不显示会造成内容变更的完整创建工具。"}
    D6S8 --> D6Alt
    D6Err{"异常：1. 创建、上传或生成失败时，系统保留已有内容并显示失败反馈。；2. 权限在操作中变化时<br/>，系统停止写入并提示无权限。；3. 用户拖拽到不可用区域时，系统不创建内容或回到可放置状<br/>态。"}
    D6S8 --> D6Err
    D6Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D6S8 --> D6Perm
  end
  subgraph D7G["通过快捷键创建图表"]
    D7Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D7Entry["可见入口/区域：用户进入可编辑白板后，在画布边缘看到 Board Menu 工具栏。；当前 Boar<br/>d Menu 直接渲染的入口包括选择、平移、便利贴、手绘、文本、连接线、形状、资源和<br/>模板。；当前 Board Menu 未直接渲染文件上传、链接、图表或 AI 助手按钮<br/>；这些能力只有在其它可见入口明确展示时，才属于用户可操作能力。；画布快捷键中注册了 <br/>C 键切换图表模式。"]
    D7Start --> D7Entry
    D7S1["1. 用户打开可编辑 Board，系统显示 Board Menu，但不显示图表按钮。"]
    D7Entry --> D7S1
    D7S2["2. 用户确认当前没有在文本输入框、便利贴编辑态或其它输入区域中输入内容。"]
    D7S1 --> D7S2
    D7S3["3. 用户按 C 键，系统把当前工具模式切换为图表模式。"]
    D7S2 --> D7S3
    D7S4["4. 用户在画布点击目标位置，系统尝试创建当前图表类型的默认图表组件。"]
    D7S3 --> D7S4
    D7S5["5. 创建成功时，系统在画布显示图表内容、图表边界和选中状态。"]
    D7S4 --> D7S5
    D7S6["6. 用户选中图表后，系统显示该图表可用的 Widget Menu 或 Context <br/>Menu 操作。"]
    D7S5 --> D7S6
    D7S7["7. 用户移动或缩放图表时，系统更新图表位置和显示大小。"]
    D7S6 --> D7S7
    D7S8["8. 用户切换到选择工具或按 Esc 时，系统退出图表模式并回到选择状态。"]
    D7S7 --> D7S8
    D7S9["9. 如果图表模式不可用或创建失败，系统不改变画布，并显示失败或不可用反馈。"]
    D7S8 --> D7S9
    D7Alt{"备选：1. 用户按 Esc、点击选择工具或切换到其他工具时，系统退出当前创建模式。；2. 工具<br/>入口不可用时，系统隐藏、禁用或提示当前无法使用。；3. Visitor 和 public<br/> visitor 不显示会造成内容变更的完整创建工具。"}
    D7S9 --> D7Alt
    D7Err{"异常：1. 创建、上传或生成失败时，系统保留已有内容并显示失败反馈。；2. 权限在操作中变化时<br/>，系统停止写入并提示无权限。；3. 用户点击不可用区域或图表创建失败时，系统不创建内容或<br/>回到可放置状态。"}
    D7S9 --> D7Err
    D7Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D7S9 --> D7Perm
  end
  subgraph D8G["上传文件"]
    D8Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D8Entry["可见入口/区域：用户进入可编辑白板后，在画布边缘看到 Board Menu 工具栏。；当前 Boar<br/>d Menu 直接渲染的入口包括选择、平移、便利贴、手绘、文本、连接线、形状、资源和<br/>模板。；当前 Board Menu 未直接渲染文件上传、链接、图表或 AI 助手按钮<br/>；这些能力只有在其它可见入口明确展示时，才属于用户可操作能力。"]
    D8Start --> D8Entry
    D8S1["1. 用户在 Board Menu 中点击资源入口，系统打开资源面板。"]
    D8Entry --> D8S1
    D8S2["2. 当前资源面板显示搜索框、图片标签和图标标签；当前 Board Menu 未直接显示<br/>文件上传入口。"]
    D8S1 --> D8S2
    D8S3["3. 如果当前环境另行显示文件上传入口，用户点击后系统打开文件选择器，并显示最大文件大小<br/>等可见限制。"]
    D8S2 --> D8S3
    D8S4["4. 用户选择一个或多个文件后，系统显示上传中状态。"]
    D8S3 --> D8S4
    D8S5["5. 上传成功后，系统在画布中心附近创建对应文件或预览对象，并显示上传成功反馈。"]
    D8S4 --> D8S5
    D8S6["6. 用户选中文件组件时，系统显示文件名、下载、音频转文本、锁定、删除或 AI 等当前适<br/>用入口。"]
    D8S5 --> D8S6
    D8S7["7. 用户可以移动、缩放或排列上传后的组件。"]
    D8S6 --> D8S7
    D8S8["8. 如果文件过大、格式不支持或网络失败，系统显示错误反馈，并保留原画布内容。"]
    D8S7 --> D8S8
    D8S9["9. 用户取消文件选择时，系统关闭选择器，不创建新内容。"]
    D8S8 --> D8S9
    D8Alt{"备选：1. 用户按 Esc、点击选择工具或切换到其他工具时，系统退出当前创建模式。；2. 工具<br/>入口不可用时，系统隐藏、禁用或提示当前无法使用。；3. Visitor 和 public<br/> visitor 不显示会造成内容变更的完整创建工具。"}
    D8S9 --> D8Alt
    D8Err{"异常：1. 创建、上传或生成失败时，系统保留已有内容并显示失败反馈。；2. 权限在操作中变化时<br/>，系统停止写入并提示无权限。；3. 用户拖拽到不可用区域时，系统不创建内容或回到可放置状<br/>态。"}
    D8S9 --> D8Err
    D8Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D8S9 --> D8Perm
  end
```

```mermaid
flowchart TD
  subgraph D9G["使用资源和模板"]
    D9Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D9Entry["可见入口/区域：用户进入可编辑白板后，在画布边缘看到 Board Menu 工具栏。；当前 Boar<br/>d Menu 直接渲染的入口包括选择、平移、便利贴、手绘、文本、连接线、形状、资源和<br/>模板。；当前 Board Menu 未直接渲染文件上传、链接、图表或 AI 助手按钮<br/>；这些能力只有在其它可见入口明确展示时，才属于用户可操作能力。"]
    D9Start --> D9Entry
    D9S1["1. 用户在 Board Menu 中看到资源、模板或素材入口。"]
    D9Entry --> D9S1
    D9S2["2. 用户点击资源入口，系统打开资源面板，并展示搜索框、图片和图标标签。"]
    D9S1 --> D9S2
    D9S3["3. 用户可以输入关键词搜索、切换分类、滚动列表或查看空状态。"]
    D9S2 --> D9S3
    D9S4["4. 用户点击资源卡片或把资源拖到画布，系统显示放置预览或加载状态。"]
    D9S3 --> D9S4
    D9S5["5. 资源加载成功后，系统在画布创建对应图片或图标资源对象。"]
    D9S4 --> D9S5
    D9S6["6. 用户选择模板时，系统按模板结构向画布插入一组内容，并显示选中或完成反馈。"]
    D9S5 --> D9S6
    D9S7["7. 用户可以关闭资源面板，系统保留已放置内容并恢复画布操作。"]
    D9S6 --> D9S7
    D9S8["8. 如果资源加载失败、无结果或权限不足，系统显示空状态、失败提示或隐藏不可用入口。"]
    D9S7 --> D9S8
    D9Alt{"备选：1. 用户按 Esc、点击选择工具或切换到其他工具时，系统退出当前创建模式。；2. 工具<br/>入口不可用时，系统隐藏、禁用或提示当前无法使用。；3. Visitor 和 public<br/> visitor 不显示会造成内容变更的完整创建工具。"}
    D9S8 --> D9Alt
    D9Err{"异常：1. 创建、上传或生成失败时，系统保留已有内容并显示失败反馈。；2. 权限在操作中变化时<br/>，系统停止写入并提示无权限。；3. 用户拖拽到不可用区域时，系统不创建内容或回到可放置状<br/>态。"}
    D9S8 --> D9Err
    D9Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D9S8 --> D9Perm
  end
  subgraph D10G["使用 Board AI 助手"]
    D10Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D10Entry["可见入口/区域：用户进入 Board 后，系统在画布上显示 Chat AI 浮动按钮。；AI 聊天侧<br/>栏未打开且当前不在演示模式时，浮动按钮可见。；用户点击浮动按钮后，系统打开 Boar<br/>d Chat Sidebar。"]
    D10Start --> D10Entry
    D10S1["1. 用户在 Board 画布上看到 Chat AI 浮动按钮，按钮图标显示为 AI 头<br/>像。"]
    D10Entry --> D10S1
    D10S2["2. 用户悬停按钮时，系统显示 BoardX AI Assistant 提示。"]
    D10S1 --> D10S2
    D10S3["3. 用户点击按钮，系统打开 Board Chat Sidebar，并隐藏浮动按钮。"]
    D10S2 --> D10S3
    D10S4["4. 系统在侧栏中显示聊天区域 Header、聊天内容区和消息输入区。"]
    D10S3 --> D10S4
    D10S5["5. 用户可在 Header 中打开聊天列表、切换或新建 Board 聊天线程，并可关闭<br/>侧栏。"]
    D10S4 --> D10S5
    D10S6["6. 用户在消息输入区输入问题、指令或上下文请求。"]
    D10S5 --> D10S6
    D10S7["7. 用户发送消息后，系统在聊天内容区显示用户消息、AI 回复加载状态和 AI 返回内容<br/>。"]
    D10S6 --> D10S7
    D10S8["8. 用户拖拽侧栏左侧调整宽度时，系统实时改变侧栏宽度，并保持画布可见。"]
    D10S7 --> D10S8
    D10S9["9. 用户关闭侧栏后，系统收起 Board Chat Sidebar，并重新显示 Cha<br/>t AI 浮动按钮。"]
    D10S8 --> D10S9
    D10S10["10. 如果 Credit 不足、AI 处理失败或权限不足，系统显示对应提示，不覆盖原画<br/>布内容。"]
    D10S9 --> D10S10
    D10Alt{"备选：1. 已打开 Board Chat Sidebar 时，系统不再显示 Chat AI 浮<br/>动按钮。；2. 进入演示模式时，系统隐藏 Chat AI 浮动按钮。；3. 用户没有编辑<br/>权限时，可以按访问策略使用查看或对话能力，但不能通过 AI 结果直接改写 Board 内<br/>容。"}
    D10S10 --> D10Alt
    D10Err{"异常：1. 聊天线程加载失败时，系统保留侧栏并展示失败反馈。；2. 消息发送失败时，系统保留用<br/>户输入或失败消息，并允许用户重试。；3. 权限在操作中变化时，系统停止会改变 Board<br/> 内容的动作并提示无权限。"}
    D10S10 --> D10Err
    D10Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D10S10 --> D10Perm
  end
  subgraph D11G["创建链接组件"]
    D11Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D11Entry["可见入口/区域：用户进入可编辑白板后，在画布边缘看到 Board Menu 工具栏。；当前 Boar<br/>d Menu 直接渲染的入口包括选择、平移、便利贴、手绘、文本、连接线、形状、资源和<br/>模板。；当前 Board Menu 未直接渲染文件上传、链接、图表或 AI 助手按钮<br/>；这些能力只有在其它可见入口明确展示时，才属于用户可操作能力。"]
    D11Start --> D11Entry
    D11S1["1. 用户在 Board Menu 中点击资源入口，系统打开资源面板。"]
    D11Entry --> D11S1
    D11S2["2. 当前资源面板显示搜索框、图片标签和图标标签；当前 Board Menu 未直接显示<br/>添加链接入口。"]
    D11S1 --> D11S2
    D11S3["3. 用户在资源面板的图片、图标标签和搜索结果中继续查找链接入口，系统仍不展示添加链接操<br/>作。"]
    D11S2 --> D11S3
    D11S4["4. 用户关闭资源面板或切换到其它 Board Menu 工具，系统返回画布，不创建任何<br/>链接组件。"]
    D11S3 --> D11S4
    D11S5["5. 如果当前环境另行展示链接输入入口，用户点击后系统打开链接输入框。"]
    D11S4 --> D11S5
    D11S6["6. 用户输入 URL；系统校验 URL 是否为空、格式是否可用，格式不可用时在输入框中<br/>提示用户修改。"]
    D11S5 --> D11S6
    D11S7["7. 用户确认后，系统只在当前可见流程内展示处理中、成功或失败反馈；只有画布实际出现链接<br/>组件时，用户才能继续选中、移动或删除该组件。"]
    D11S6 --> D11S7
    D11Alt{"备选：1. 用户按 Esc、点击选择工具或切换到其他工具时，系统退出当前创建模式。；2. 工具<br/>入口不可用时，系统隐藏、禁用或提示当前无法使用。；3. Visitor 和 public<br/> visitor 不显示会造成内容变更的完整创建工具。"}
    D11S7 --> D11Alt
    D11Err{"异常：1. 创建、上传或生成失败时，系统保留已有内容并显示失败反馈。；2. 权限在操作中变化时<br/>，系统停止写入并提示无权限。；3. 用户拖拽到不可用区域时，系统不创建内容或回到可放置状<br/>态。"}
    D11S7 --> D11Err
    D11Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D11S7 --> D11Perm
  end
  subgraph D12G["擦除手绘内容"]
    D12Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D12Entry["可见入口/区域：用户进入可编辑白板后，在画布边缘看到 Board Menu 工具栏。；当前 Boar<br/>d Menu 直接渲染的入口包括选择、平移、便利贴、手绘、文本、连接线、形状、资源和<br/>模板。；当前 Board Menu 未直接渲染文件上传、链接、图表或 AI 助手按钮<br/>；这些能力只有在其它可见入口明确展示时，才属于用户可操作能力。"]
    D12Start --> D12Entry
    D12S1["1. 用户在 Board Menu 的手绘相关入口中看到橡皮擦。"]
    D12Entry --> D12S1
    D12S2["2. 用户选择橡皮擦，系统高亮橡皮擦工具，并进入擦除模式。"]
    D12S1 --> D12S2
    D12S3["3. 用户切换到橡皮擦后，系统显示橡皮擦光标，并让非手绘对象不可编辑。"]
    D12S2 --> D12S3
    D12S4["4. 用户按住并拖动经过手绘笔迹，系统实时移除或标记将被擦除的内容。"]
    D12S3 --> D12S4
    D12S5["5. 用户松开后，系统更新画布并同步擦除结果。"]
    D12S4 --> D12S5
    D12S6["6. 用户擦到不可擦除对象时，系统不改变该对象，或提示该对象不能通过橡皮擦删除。"]
    D12S5 --> D12S6
    D12S7["7. 用户切换到选择、手绘或其它工具时，系统退出擦除模式。"]
    D12S6 --> D12S7
    D12S8["8. 如果权限不足或协作中对象被锁定，系统停止擦除并显示无权限或锁定提示。"]
    D12S7 --> D12S8
    D12Alt{"备选：1. 用户按 Esc、点击选择工具或切换到其他工具时，系统退出当前创建模式。；2. 工具<br/>入口不可用时，系统隐藏、禁用或提示当前无法使用。；3. Visitor 和 public<br/> visitor 不显示会造成内容变更的完整创建工具。"}
    D12S8 --> D12Alt
    D12Err{"异常：1. 创建、上传或生成失败时，系统保留已有内容并显示失败反馈。；2. 权限在操作中变化时<br/>，系统停止写入并提示无权限。；3. 用户拖拽到不可用区域时，系统不创建内容或回到可放置状<br/>态。"}
    D12S8 --> D12Err
    D12Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D12S8 --> D12Perm
  end
```

