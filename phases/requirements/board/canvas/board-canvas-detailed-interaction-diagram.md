# Board Canvas 详细交互图

本图按 Board Canvas 模块当前 Use Case 的主流程展开，重点表达每个界面能看到什么、能操作什么、操作后出现什么状态。若某个 UC 继续细化，应该同步更新该 UC 的主流程和本图。

## 模块交互展开

该部分保留当前模块已有交互图，用于表达模块内主要页面区域之间的流转。

```mermaid
flowchart TD
  Canvas["Canvas"] --> Pan["拖拽平移"]
  Canvas --> Zoom["缩放控件或滚轮缩放"]
  Canvas --> Fit["适配屏幕"]
  Canvas --> Select["点击选择组件"]
  Canvas --> MultiSelect["框选或多选组件"]
  Canvas --> Keyboard["键盘操作"]
  Canvas --> Guidelines["自动对齐参考线"]

  Pan --> ViewChanged["视角位置改变"]
  Zoom --> ZoomChanged["缩放比例改变"]
  Fit --> FitResult["画布内容适配到视野"]
  Select --> SelectionBox["显示选中框和控制点"]
  MultiSelect --> MultiState["显示多选框和多选菜单"]
  Keyboard --> KeyboardResult["移动、复制、粘贴、删除、撤销或重做"]
  Guidelines --> SnapResult["拖拽时显示参考线并辅助对齐"]
```

## Use Case 主流程展开

```mermaid
flowchart TD
  subgraph D1G["缩放画布并使用缩放控制条"]
    D1Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D1Entry["可见入口/区域：用户打开 Board 后，在画布右下角看到缩放控制条；当前界面没有展示可拖拽定位的缩<br/>略画布小地图。；缩放控制条包含适应屏幕、缩小、当前百分比下拉和放大入口。；百分比下拉<br/>展示固定缩放值 50%、70%、100%、150%、200%，并提供适应屏幕入口。"]
    D1Start --> D1Entry
    D1S1["1. 用户查看右下角缩放控制条，系统显示当前缩放百分比。"]
    D1Entry --> D1S1
    D1S2["2. 用户点击放大或缩小，系统以当前视角中心缩放画布，并立即更新百分比。"]
    D1S1 --> D1S2
    D1S3["3. 用户打开百分比下拉，系统显示可选缩放值和适应屏幕动作。"]
    D1S2 --> D1S3
    D1S4["4. 用户选择 50%、70%、100%、150% 或 200%，系统切换到对应比例，画<br/>布内容按新比例显示。"]
    D1S3 --> D1S4
    D1S5["5. 用户点击适应屏幕，系统把画布视角调整到能看到当前内容的范围，并更新百分比。"]
    D1S4 --> D1S5
    D1S6["6. 用户继续平移或缩放时，系统保持右下角百分比与画布视角一致。"]
    D1S5 --> D1S6
    D1S7["7. 缩放结束后，用户仍停留在同一 Board，已有选中对象、在线头像和协作状态继续可见<br/>。"]
    D1S6 --> D1S7
    D1Alt{"备选：1. 用户通过触控板或鼠标滚轮缩放时，系统同样更新视角和百分比。；2. 用户选择 100<br/>% 时，系统恢复到标准比例。；3. 演示模式下隐藏缩放控制时，用户只能使用当前模式允许的<br/>导航能力。"}
    D1S7 --> D1Alt
    D1Err{"异常：1. 画布尚未加载完成时，系统展示加载状态并阻止误操作。；2. 网络或同步异常时，系统显<br/>示异常状态并保留本地可恢复信息。；3. 缩放比例低于或高于系统允许范围时，系统停在可用边<br/>界，不继续缩放。"}
    D1S7 --> D1Err
    D1Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D1S7 --> D1Perm
  end
  subgraph D2G["使用对齐参考线"]
    D2Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D2Entry["可见入口/区域：用户打开 Board 后看到画布上的已有组件。；用户选中并拖动组件时，画布显示选中框<br/>和移动反馈。；当组件靠近可对齐位置时，画布可显示对齐参考线或吸附反馈。"]
    D2Start --> D2Entry
    D2S1["1. 用户点击一个可编辑组件，系统显示选中框和控制点。"]
    D2Entry --> D2S1
    D2S2["2. 用户拖动组件，系统让组件跟随指针移动。"]
    D2S1 --> D2S2
    D2S3["3. 组件接近其它对象的边缘、中心或可对齐位置时，系统显示参考线或吸附提示。"]
    D2S2 --> D2S3
    D2S4["4. 用户继续拖动，系统持续更新组件位置和参考线状态。"]
    D2S3 --> D2S4
    D2S5["5. 用户释放鼠标或触控操作，系统把组件停留在当前位置。"]
    D2S4 --> D2S5
    D2S6["6. 如果触发吸附，组件停在对齐后的位置；如果没有触发吸附，组件停在用户释放的位置。"]
    D2S5 --> D2S6
    D2S7["7. 操作结束后，系统保留选中状态，并在协作场景中让其他在线用户看到最终位置。"]
    D2S6 --> D2S7
    D2Alt{"备选：1. 用户拖动多个已选组件时，系统按多选整体显示移动和对齐反馈。；2. 用户按 Esc <br/>或点击空白处时，系统退出当前选择状态。；3. 对象被锁定时，系统限制移动，不显示可编辑的<br/>拖动结果。"}
    D2S7 --> D2Alt
    D2Err{"异常：1. 画布尚未加载完成时，系统展示加载状态并阻止误操作。；2. 网络或同步异常时，系统显<br/>示异常状态并保留本地可恢复信息。；3. 剪贴板、文件或浏览器权限不可用时，系统提示原因。"}
    D2S7 --> D2Err
    D2Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D2S7 --> D2Perm
  end
  subgraph D3G["用键盘操作组件"]
    D3Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D3Entry["可见入口/区域：用户打开 Board 后看到画布、主工具栏和已有组件。；用户选中组件后看到选中框、控<br/>制点和 Widget Menu。；用户可以使用键盘切换工具、临时平移、取消操作或打开<br/>上下文菜单。"]
    D3Start --> D3Entry
    D3S1["1. 用户按 V，系统切换到选择工具，画布允许点击、框选和移动组件。"]
    D3Entry --> D3S1
    D3S2["2. 用户按 T、D、S、N 或 L，系统分别切换到文本、手绘、形状、便利贴或连接线工具<br/>；主工具栏状态同步变化。"]
    D3S1 --> D3S2
    D3S3["3. 用户按 C 时，系统切换到图表模式；由于 Board Menu 当前不直接渲染图表<br/>按钮，系统只更新可感知的工具状态或后续创建结果，不把图表按钮显示作为前置条件。"]
    D3S2 --> D3S3
    D3S4["4. 用户按住空格，系统临时进入平移状态，光标变为抓手，拖动画布时只改变视角。"]
    D3S3 --> D3S4
    D3S5["5. 用户松开空格，系统退出临时平移，恢复到之前的工具。"]
    D3S4 --> D3S5
    D3S6["6. 用户按 Esc，系统取消当前工具或编辑状态，并回到选择状态。"]
    D3S5 --> D3S6
    D3S7["7. 用户在选中组件时按上下文菜单键，系统在选中对象附近打开 Context Menu。"]
    D3S6 --> D3S7
    D3S8["8. 用户从菜单执行复制、删除、锁定等动作后，系统更新组件状态并给出可见反馈。"]
    D3S7 --> D3S8
    D3Alt{"备选：1. 用户正在输入文本时，系统把字母键当作文本输入，不触发工具切换。；2. 对象被锁定时<br/>，键盘动作不能移动、编辑或删除该对象。；3. 只读用户可以使用导航类快捷键，但不能通过快<br/>捷键创建或修改内容。"}
    D3S8 --> D3Alt
    D3Err{"异常：1. 画布尚未加载完成时，系统展示加载状态并阻止误操作。；2. 网络或同步异常时，系统显<br/>示异常状态并保留本地可恢复信息。；3. 剪贴板、文件或浏览器权限不可用时，系统提示原因。"}
    D3S8 --> D3Err
    D3Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D3S8 --> D3Perm
  end
  subgraph D4G["选择和多选组件"]
    D4Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D4Entry["可见入口/区域：用户打开 Board 后看到画布上的便利贴、形状、连接线、文本、图片、文件、手绘或图<br/>表等组件。；用户点击单个组件时看到选中框、控制点和对应 Widget Menu。；用<br/>户框选或多选组件时看到整体选中边界和多选可用菜单。"]
    D4Start --> D4Entry
    D4S1["1. 用户点击画布上的单个组件，系统显示该组件的选中框和控制点。"]
    D4Entry --> D4S1
    D4S2["2. 系统根据组件类型显示可用 Widget Menu，例如文本样式、颜色、线宽、裁剪、<br/>下载、锁定或连接线端点。"]
    D4S1 --> D4S2
    D4S3["3. 用户拖动选中组件，系统移动组件并显示位置变化。"]
    D4S2 --> D4S3
    D4S4["4. 用户拖动控制点，系统按组件类型调整大小、路径、显示区域或文字排版区域。"]
    D4S3 --> D4S4
    D4S5["5. 用户在空白区域拖出选择框，系统把框内可选组件加入多选，并显示整体选中边界。"]
    D4S4 --> D4S5
    D4S6["6. 用户对多选对象执行移动、对齐、分布、编组、锁定或删除，系统只展示多选适用动作并更新<br/>结果。"]
    D4S5 --> D4S6
    D4S7["7. 用户点击空白处或按 Esc，系统取消选择并隐藏相关菜单。"]
    D4S6 --> D4S7
    D4S8["8. 操作结束后，系统保留最终组件位置、尺寸、选中状态或多选状态。"]
    D4S7 --> D4S8
    D4Alt{"备选：1. 用户多选中包含不同组件类型时，系统只展示这些组件共同支持的动作。；2. 对象被锁定<br/>时，系统可显示其存在，但限制移动、缩放、改内容和删除。；3. 只读用户可以选择或查看允许<br/>范围内的内容，但不能保存编辑结果。"}
    D4S8 --> D4Alt
    D4Err{"异常：1. 画布尚未加载完成时，系统展示加载状态并阻止误操作。；2. 网络或同步异常时，系统显<br/>示异常状态并保留本地可恢复信息。；3. 剪贴板、文件或浏览器权限不可用时，系统提示原因。"}
    D4S8 --> D4Err
    D4Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D4S8 --> D4Perm
  end
```

