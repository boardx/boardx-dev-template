# Board Collaboration 详细交互图

本图按 Board Collaboration 模块当前 Use Case 的主流程展开，重点表达每个界面能看到什么、能操作什么、操作后出现什么状态。若某个 UC 继续细化，应该同步更新该 UC 的主流程和本图。

## 模块交互展开

该部分保留当前模块已有交互图，用于表达模块内主要页面区域之间的流转。

```mermaid
flowchart TD
  Board["多人打开同一 Board"] --> Sync["显示同步状态"]
  Board --> Online["显示在线用户头像"]
  Board --> Cursor["显示协作者光标或选择状态"]
  Online --> Follow["选择跟随协作者"]
  Follow --> Following["当前视角跟随目标用户"]
  Following --> StopFollow["用户手动移动视角或取消跟随"]
  StopFollow --> Board
  Sync --> Synced["已同步"]
  Sync --> Syncing["同步中"]
  Sync --> Conflict["对象被他人删除、锁定或权限变化"]
  Conflict --> Refresh["关闭当前编辑状态并刷新画布状态"]
  Cursor --> Awareness["看到协作者位置、选择或编辑状态"]
```

## Use Case 主流程展开

```mermaid
flowchart TD
  subgraph D1G["实时同步协作内容"]
    D1Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D1Entry["可见入口/区域：用户进入 Board 后，在 Header 区域看到在线成员头像。；多人同时在线时，<br/>头像区域显示当前用户、其他在线用户和“更多在线用户”入口。；画布中可看到协作者创建、<br/>移动、修改或删除后的组件变化。；只读用户可以观察同步结果，但不能编辑内容。"]
    D1Start --> D1Entry
    D1S1["1. 用户打开 Board，系统加载画布内容、Header、主工具栏和在线成员区域。"]
    D1Entry --> D1S1
    D1S2["2. 系统在在线成员区域显示当前用户头像；其他用户进入后，头像列表增加。"]
    D1S1 --> D1S2
    D1S3["3. 用户创建、移动、缩放、编辑、锁定或删除组件，系统先在本机画布显示结果。"]
    D1S2 --> D1S3
    D1S4["4. 其他在线协作者看到对应组件变化，例如新便利贴出现、文本更新、图片移动或对象被删除。"]
    D1S3 --> D1S4
    D1S5["5. 其他协作者编辑同一 Board 时，用户也能在自己的画布上看到变化。"]
    D1S4 --> D1S5
    D1S6["6. 在线人数超过头像可见数量时，用户点击“更多”头像，系统打开在线用户列表。"]
    D1S5 --> D1S6
    D1S7["7. 同步完成后，用户继续停留在当前 Board，组件状态与协作者的最新操作一致。"]
    D1S6 --> D1S7
    D1Alt{"备选：1. 协作者离线时，头像从在线成员区域移除。；2. 网络短暂中断时，系统显示连接或同步异<br/>常状态；恢复后继续同步最新状态。；3. Visitor 只能查看同步结果，不能通过协作同<br/>步获得编辑能力。"}
    D1S7 --> D1Alt
    D1Err{"异常：1. 实时连接失败时，系统提示连接异常并保留只读或本地状态。；2. 权限变更后，系统重新<br/>判断是否允许继续查看或编辑。"}
    D1S7 --> D1Err
    D1Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D1S7 --> D1Perm
  end
  subgraph D2G["查看在线成员和光标"]
    D2Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D2Entry["可见入口/区域：用户进入 Board 后，在 Header 区域看到在线成员头像。；头像区域优先显示<br/>当前用户和部分其他在线用户；人数较多时显示“+N”更多入口。；协作者移动鼠标时，画布<br/>可显示对方的光标位置和用户标识。"]
    D2Start --> D2Entry
    D2S1["1. 用户打开 Board，系统在 Header 中显示自己的头像。"]
    D2Entry --> D2S1
    D2S2["2. 其他用户进入同一 Board 后，系统把其头像加入在线成员区域。"]
    D2S1 --> D2S2
    D2S3["3. 用户把鼠标移到头像上，系统显示该成员名称或头像提示。"]
    D2S2 --> D2S3
    D2S4["4. 在线成员超过可见数量时，用户点击“+N”，系统打开更多在线用户弹层，列表展示成员头<br/>像和名称。"]
    D2S3 --> D2S4
    D2S5["5. 协作者在画布上移动鼠标，系统在画布对应位置显示协作者光标。"]
    D2S4 --> D2S5
    D2S6["6. 协作者停止移动、离开画布或离线时，系统隐藏或移除对应光标。"]
    D2S5 --> D2S6
    D2S7["7. 用户通过在线头像和光标判断谁正在同一 Board 中协作。"]
    D2S6 --> D2S7
    D2Alt{"备选：1. 小屏幕下系统减少直接展示的头像数量，并把更多成员放入“+N”弹层。；2. 协作者没<br/>有头像时，系统显示名称缩写或默认头像。；3. Visitor 可以看到在线状态和光标，但<br/>不能编辑 Board 内容。"}
    D2S7 --> D2Alt
    D2Err{"异常：1. 实时连接失败时，系统提示连接异常并保留只读或本地状态。；2. 权限变更后，系统重新<br/>判断是否允许继续查看或编辑。"}
    D2S7 --> D2Err
    D2Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D2S7 --> D2Perm
  end
  subgraph D3G["跟随协作者视角"]
    D3Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D3Entry["可见入口/区域：用户进入 Board 后，在 Header 区域看到在线成员头像和跟随相关入口。；启<br/>用跟随后，画布顶部显示跟随状态条。；跟随状态条显示正在被跟随的人或“成员正在跟随你”<br/>，并提供暂停、恢复和停止入口。"]
    D3Start --> D3Entry
    D3S1["1. 用户在在线成员或跟随入口中选择要跟随的协作者。"]
    D3Entry --> D3S1
    D3S2["2. 系统把用户画布视角移动到协作者当前视角，并显示跟随状态条。"]
    D3S1 --> D3S2
    D3S3["3. 协作者平移或缩放画布时，系统同步调整用户的画布视角。"]
    D3S2 --> D3S3
    D3S4["4. 用户点击暂停，系统保留当前跟随状态条，但停止继续套用对方视角变化。"]
    D3S3 --> D3S4
    D3S5["5. 用户点击恢复，系统继续按协作者视角更新自己的画布。"]
    D3S4 --> D3S5
    D3S6["6. 用户点击停止，系统退出跟随状态，状态条消失，用户保留当前视角。"]
    D3S5 --> D3S6
    D3S7["7. 如果用户是被跟随者，系统显示“成员正在跟随你”一类提示，让用户知道自己的视角正在影<br/>响他人。"]
    D3S6 --> D3S7
    D3Alt{"备选：1. 被跟随协作者离线或停止广播视角时，系统退出或停止更新跟随状态。；2. 用户手动平移<br/>或缩放时，系统可暂停或退出跟随，以用户主动视角为准。；3. Visitor 可以跟随查看<br/>视角，但不能因为跟随获得编辑能力。"}
    D3S7 --> D3Alt
    D3Err{"异常：1. 实时连接失败时，系统提示连接异常并保留只读或本地状态。；2. 权限变更后，系统重新<br/>判断是否允许继续查看或编辑。"}
    D3S7 --> D3Err
    D3Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D3S7 --> D3Perm
  end
```

