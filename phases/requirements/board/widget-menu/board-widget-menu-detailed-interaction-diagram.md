# Board Widget Menu 详细交互图

本图按 Board Widget Menu 模块当前 Use Case 的主流程展开，重点表达每个界面能看到什么、能操作什么、操作后出现什么状态。若某个 UC 继续细化，应该同步更新该 UC 的主流程和本图。

## 模块交互展开

该部分保留当前模块已有交互图，用于表达模块内主要页面区域之间的流转。

```mermaid
flowchart TD
  Select["选中组件"] --> Menu{"按类型、数量、锁定和权限展示菜单"}
  Menu --> TextStyle["文本样式：字体、字号、字重、对齐、颜色"]
  Menu --> Fill["填充、边框、线宽"]
  Menu --> ConnectorStyle["连接线颜色、线宽、端点、直线或曲线"]
  Menu --> ImageCrop["图片裁剪"]
  Menu --> FileDownload["文件下载"]
  Menu --> AudioText["音频转文本"]
  Menu --> AI["组件 AI 助手"]
  Menu --> Align["多选对齐或整理"]
  Menu --> Format["应用格式"]
  Menu --> Lock["锁定或解锁"]
  Menu --> Delete["删除"]

  TextStyle --> StyleResult["组件文字样式即时更新"]
  Fill --> FillResult["组件外观即时更新"]
  ConnectorStyle --> ConnectorResult["连接线样式更新并保留连接关系"]
  ImageCrop --> CropResult["图片裁剪结果应用或取消"]
  FileDownload --> DownloadResult["开始下载或显示失败"]
  AudioText --> TranscriptResult["生成转写文本或失败"]
  AI --> AIResult["生成建议、更新内容或失败"]
  Align --> AlignResult["选中组件位置更新"]
  Format --> FormatResult["样式应用到目标组件"]
  Lock --> LockResult["编辑入口隐藏或恢复"]
  Delete --> DeleteResult["组件从画布移除"]
```

## Use Case 主流程展开

```mermaid
flowchart TD
  subgraph D1G["使用 Widget Menu"]
    D1Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D1Entry["可见入口/区域：用户在画布上选中单个组件或框选多个组件后，选中框附近出现悬浮 Widget Menu<br/>。；菜单按对象类型显示有限入口：文本/便利贴可显示字号、字体、字重、对齐、背景色、文<br/>字色、格式应用、切换便利贴类型；连接线可显示颜色、线宽、端点和直线/曲线；图片可显示<br/>裁剪；文件可显示文件名、下载，音频文件可显示转文本。；多数可编辑对象可显示锁定、删除<br/>和 AI 助手；锁定对象通常只保留锁定状态入口。"]
    D1Start --> D1Entry
    D1S1["1. 用户单击对象或框选多个对象，系统在选中框附近显示悬浮 Widget Menu。"]
    D1Entry --> D1S1
    D1S2["2. 系统按对象类型展示菜单：文本/便利贴显示字号、字体、字重、对齐、背景色、文字色、格<br/>式应用或类型切换；连接线显示颜色、线宽、端点和直线/曲线；图片显示裁剪；文件显示文件名和<br/>下载；音频文件显示转文本。"]
    D1S1 --> D1S2
    D1S3["3. 系统同时按选中数量、锁定状态和权限展示锁定、删除、AI 助手、对齐或整理等入口。"]
    D1S2 --> D1S3
    D1S4["4. 用户点击某个入口时，系统打开下拉、颜色面板、确认弹窗、处理状态或直接执行动作。"]
    D1S3 --> D1S4
    D1S5["5. 用户调整样式后，系统即时更新选中对象，并保持对象选中。"]
    D1S4 --> D1S5
    D1S6["6. 用户点击画布空白处、切换工具或取消选择时，系统隐藏 Widget Menu。"]
    D1S5 --> D1S6
    D1S7["7. 如果对象被锁定，系统隐藏会改变内容的入口，只保留锁定状态入口。"]
    D1S6 --> D1S7
    D1S8["8. 如果用户没有编辑权限，系统不显示会改变对象的完整菜单。"]
    D1S7 --> D1S8
    D1Alt{"备选：1. 多选时，系统只展示多选适用的锁定、删除、AI、对齐或整理类入口。；2. 当前组件类<br/>型不支持的动作不显示。；3. 对象被锁定时，系统隐藏会改变内容的入口，或要求先解锁。"}
    D1S8 --> D1Alt
    D1Err{"异常：1. 权限不足时，系统不执行修改并提示无权限。；2. 转换、刷新、下载、裁剪或 AI 处<br/>理失败时，系统保留原组件并显示失败反馈。；3. 组件在操作期间被协作者删除或权限改变时，<br/>系统关闭菜单并刷新状态。"}
    D1S8 --> D1Err
    D1Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D1S8 --> D1Perm
  end
  subgraph D2G["调整组件样式"]
    D2Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D2Entry["可见入口/区域：用户在画布上选中单个组件或框选多个组件后，选中框附近出现悬浮 Widget Menu<br/>。；菜单按对象类型显示有限入口：文本/便利贴可显示字号、字体、字重、对齐、背景色、文<br/>字色、格式应用、切换便利贴类型；连接线可显示颜色、线宽、端点和直线/曲线；图片可显示<br/>裁剪；文件可显示文件名、下载，音频文件可显示转文本。；多数可编辑对象可显示锁定、删除<br/>和 AI 助手；锁定对象通常只保留锁定状态入口。"]
    D2Start --> D2Entry
    D2S1["1. 用户选中文本、便利贴、形状、连接线或其它支持样式的对象，系统显示对象适用的样式入口<br/>。"]
    D2Entry --> D2S1
    D2S2["2. 用户打开颜色入口，系统显示当前可用色板或颜色选择器。"]
    D2S1 --> D2S2
    D2S3["3. 用户选择背景色、文字色、边框色或连接线颜色，系统即时更新对象外观。"]
    D2S2 --> D2S3
    D2S4["4. 用户打开线宽、边框或端点入口，系统展示当前可见选项。"]
    D2S3 --> D2S4
    D2S5["5. 用户选择线宽、边框粗细、箭头端点或路径形态，系统更新对象显示。"]
    D2S4 --> D2S5
    D2S6["6. 用户打开文本样式入口并选择字号、字体、字重或对齐方式，系统更新对象内文字。"]
    D2S5 --> D2S6
    D2S7["7. 多选对象时，系统只显示共同适用的样式项；不同值可以显示混合状态。"]
    D2S6 --> D2S7
    D2S8["8. 锁定、只读或无权限对象不允许通过样式入口被修改。"]
    D2S7 --> D2S8
    D2Alt{"备选：1. 多选时，系统只展示多选适用的锁定、删除、AI、对齐或整理类入口。；2. 当前组件类<br/>型不支持的动作不显示。；3. 对象被锁定时，系统隐藏会改变内容的入口，或要求先解锁。"}
    D2S8 --> D2Alt
    D2Err{"异常：1. 权限不足时，系统不执行修改并提示无权限。；2. 转换、刷新、下载、裁剪或 AI 处<br/>理失败时，系统保留原组件并显示失败反馈。；3. 组件在操作期间被协作者删除或权限改变时，<br/>系统关闭菜单并刷新状态。"}
    D2S8 --> D2Err
    D2Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D2S8 --> D2Perm
  end
  subgraph D3G["锁定或解锁组件"]
    D3Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D3Entry["可见入口/区域：用户在画布上选中单个组件或框选多个组件后，选中框附近出现悬浮 Widget Menu<br/>。；菜单按对象类型显示有限入口：文本/便利贴可显示字号、字体、字重、对齐、背景色、文<br/>字色、格式应用、切换便利贴类型；连接线可显示颜色、线宽、端点和直线/曲线；图片可显示<br/>裁剪；文件可显示文件名、下载，音频文件可显示转文本。；多数可编辑对象可显示锁定、删除<br/>和 AI 助手；锁定对象通常只保留锁定状态入口。"]
    D3Start --> D3Entry
    D3S1["1. 用户选中未锁定对象，系统在 Widget Menu 中显示锁定入口。"]
    D3Entry --> D3S1
    D3S2["2. 用户点击锁定，系统把对象切换为锁定状态，并更新对象边界或锁定标记。"]
    D3S1 --> D3S2
    D3S3["3. 锁定后，系统隐藏或禁用移动、缩放、旋转、文本编辑、样式修改和删除等入口。"]
    D3S2 --> D3S3
    D3S4["4. 用户再次选中锁定对象，系统只显示锁定状态入口，用户可通过该入口切换回未锁定状态。"]
    D3S3 --> D3S4
    D3S5["5. 用户点击解锁，系统恢复普通选中框、控制点和可编辑菜单。"]
    D3S4 --> D3S5
    D3S6["6. 多选对象时，系统可以对允许对象批量锁定或解锁，并对不可操作对象显示部分失败反馈。"]
    D3S5 --> D3S6
    D3S7["7. 如果用户没有权限，系统不改变锁定状态，并提示无权限。"]
    D3S6 --> D3S7
    D3S8["8. 锁定状态变化后，其他协作者也应看到对象可操作状态变化。"]
    D3S7 --> D3S8
    D3Alt{"备选：1. 多选时，系统只展示多选适用的锁定、删除、AI、对齐或整理类入口。；2. 当前组件类<br/>型不支持的动作不显示。；3. 对象被锁定时，系统隐藏会改变内容的入口，或要求先解锁。"}
    D3S8 --> D3Alt
    D3Err{"异常：1. 权限不足时，系统不执行修改并提示无权限。；2. 转换、刷新、下载、裁剪或 AI 处<br/>理失败时，系统保留原组件并显示失败反馈。；3. 组件在操作期间被协作者删除或权限改变时，<br/>系统关闭菜单并刷新状态。"}
    D3S8 --> D3Err
    D3Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D3S8 --> D3Perm
  end
  subgraph D4G["裁剪图片"]
    D4Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D4Entry["可见入口/区域：用户在画布上选中单个组件或框选多个组件后，选中框附近出现悬浮 Widget Menu<br/>。；菜单按对象类型显示有限入口：文本/便利贴可显示字号、字体、字重、对齐、背景色、文<br/>字色、格式应用、切换便利贴类型；连接线可显示颜色、线宽、端点和直线/曲线；图片可显示<br/>裁剪；文件可显示文件名、下载，音频文件可显示转文本。；多数可编辑对象可显示锁定、删除<br/>和 AI 助手；锁定对象通常只保留锁定状态入口。"]
    D4Start --> D4Entry
    D4S1["1. 用户选中图片组件，系统在 Widget Menu 中显示裁剪入口。"]
    D4Entry --> D4S1
    D4S2["2. 用户点击裁剪，系统进入裁剪状态，并在图片上显示裁剪边界、控制点或遮罩。"]
    D4S1 --> D4S2
    D4S3["3. 用户拖拽裁剪边界或控制点，系统实时显示裁剪范围。"]
    D4S2 --> D4S3
    D4S4["4. 用户移动图片或裁剪框时，系统更新预览区域。"]
    D4S3 --> D4S4
    D4S5["5. 用户确认裁剪，系统把图片显示更新为裁剪结果，并退出裁剪状态。"]
    D4S4 --> D4S5
    D4S6["6. 用户取消裁剪或点击画布外部，系统恢复原图显示。"]
    D4S5 --> D4S6
    D4S7["7. 如果图片正在加载、已锁定或用户无权限，系统隐藏裁剪入口或提示不可裁剪。"]
    D4S6 --> D4S7
    D4S8["8. 裁剪失败时，系统保留原图并显示失败反馈。"]
    D4S7 --> D4S8
    D4Alt{"备选：1. 多选时，系统只展示多选适用的锁定、删除、AI、对齐或整理类入口。；2. 当前组件类<br/>型不支持的动作不显示。；3. 对象被锁定时，系统隐藏会改变内容的入口，或要求先解锁。"}
    D4S8 --> D4Alt
    D4Err{"异常：1. 权限不足时，系统不执行修改并提示无权限。；2. 转换、刷新、下载、裁剪或 AI 处<br/>理失败时，系统保留原组件并显示失败反馈。；3. 组件在操作期间被协作者删除或权限改变时，<br/>系统关闭菜单并刷新状态。"}
    D4S8 --> D4Err
    D4Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D4S8 --> D4Perm
  end
```

```mermaid
flowchart TD
  subgraph D5G["下载文件组件"]
    D5Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D5Entry["可见入口/区域：用户在画布上选中单个组件或框选多个组件后，选中框附近出现悬浮 Widget Menu<br/>。；菜单按对象类型显示有限入口：文本/便利贴可显示字号、字体、字重、对齐、背景色、文<br/>字色、格式应用、切换便利贴类型；连接线可显示颜色、线宽、端点和直线/曲线；图片可显示<br/>裁剪；文件可显示文件名、下载，音频文件可显示转文本。；多数可编辑对象可显示锁定、删除<br/>和 AI 助手；锁定对象通常只保留锁定状态入口。"]
    D5Start --> D5Entry
    D5S1["1. 用户选中文件、PDF、Word、PPT、Excel、视频、音频或压缩包组件，系统显<br/>示文件名和下载入口。"]
    D5Entry --> D5S1
    D5S2["2. 用户点击下载，系统显示准备下载、下载中或浏览器下载反馈。"]
    D5S1 --> D5S2
    D5S3["3. 如果文件需要重新获取，系统保持文件组件选中，并显示处理中状态。"]
    D5S2 --> D5S3
    D5S4["4. 下载成功后，浏览器开始保存文件，画布内容不改变。"]
    D5S3 --> D5S4
    D5S5["5. 用户可以继续移动、锁定或删除该文件组件。"]
    D5S4 --> D5S5
    D5S6["6. 如果文件地址失效、权限不足或网络失败，系统显示下载失败提示。"]
    D5S5 --> D5S6
    D5S7["7. 多选时，文件下载入口只在当前选区仍共同适用时显示。"]
    D5S6 --> D5S7
    D5Alt{"备选：1. 多选时，系统只展示多选适用的锁定、删除、AI、对齐或整理类入口。；2. 当前组件类<br/>型不支持的动作不显示。；3. 对象被锁定时，系统隐藏会改变内容的入口，或要求先解锁。"}
    D5S7 --> D5Alt
    D5Err{"异常：1. 权限不足时，系统不执行修改并提示无权限。；2. 转换、刷新、下载、裁剪或 AI 处<br/>理失败时，系统保留原组件并显示失败反馈。；3. 组件在操作期间被协作者删除或权限改变时，<br/>系统关闭菜单并刷新状态。"}
    D5S7 --> D5Err
    D5Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D5S7 --> D5Perm
  end
  subgraph D6G["音频转文本"]
    D6Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D6Entry["可见入口/区域：用户在画布上选中单个组件或框选多个组件后，选中框附近出现悬浮 Widget Menu<br/>。；菜单按对象类型显示有限入口：文本/便利贴可显示字号、字体、字重、对齐、背景色、文<br/>字色、格式应用、切换便利贴类型；连接线可显示颜色、线宽、端点和直线/曲线；图片可显示<br/>裁剪；文件可显示文件名、下载，音频文件可显示转文本。；多数可编辑对象可显示锁定、删除<br/>和 AI 助手；锁定对象通常只保留锁定状态入口。"]
    D6Start --> D6Entry
    D6S1["1. 用户选中音频文件组件，系统在 Widget Menu 中显示音频转文本入口。"]
    D6Entry --> D6S1
    D6S2["2. 用户点击音频转文本，系统打开确认状态或直接进入识别处理中状态。"]
    D6S1 --> D6S2
    D6S3["3. 系统显示转写中、排队中或进度反馈，音频组件保持可见。"]
    D6S2 --> D6S3
    D6S4["4. 转写成功后，系统显示文本结果，或在画布创建文本/便利贴等承载结果的对象。"]
    D6S3 --> D6S4
    D6S5["5. 用户可以查看、编辑、移动或复制生成的文本内容。"]
    D6S4 --> D6S5
    D6S6["6. 如果用户取消处理，系统关闭处理状态并保留原音频组件。"]
    D6S5 --> D6S6
    D6S7["7. 如果音频格式不支持、时长过长、Credit 不足或识别失败，系统显示失败原因。"]
    D6S6 --> D6S7
    D6S8["8. 锁定或只读状态下，系统不允许创建转写结果到画布。"]
    D6S7 --> D6S8
    D6Alt{"备选：1. 多选时，系统只展示多选适用的锁定、删除、AI、对齐或整理类入口。；2. 当前组件类<br/>型不支持的动作不显示。；3. 对象被锁定时，系统隐藏会改变内容的入口，或要求先解锁。"}
    D6S8 --> D6Alt
    D6Err{"异常：1. 权限不足时，系统不执行修改并提示无权限。；2. 转换、刷新、下载、裁剪或 AI 处<br/>理失败时，系统保留原组件并显示失败反馈。；3. 组件在操作期间被协作者删除或权限改变时，<br/>系统关闭菜单并刷新状态。"}
    D6S8 --> D6Err
    D6Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D6S8 --> D6Perm
  end
  subgraph D7G["使用组件 AI 助手"]
    D7Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D7Entry["可见入口/区域：用户在画布上选中单个组件或框选多个组件后，选中框附近出现悬浮 Widget Menu<br/>。；菜单按对象类型显示有限入口：文本/便利贴可显示字号、字体、字重、对齐、背景色、文<br/>字色、格式应用、切换便利贴类型；连接线可显示颜色、线宽、端点和直线/曲线；图片可显示<br/>裁剪；文件可显示文件名、下载，音频文件可显示转文本。；多数可编辑对象可显示锁定、删除<br/>和 AI 助手；锁定对象通常只保留锁定状态入口。"]
    D7Start --> D7Entry
    D7S1["1. 用户选中支持 AI 的对象，系统在 Widget Menu 中显示 AI 助手入口<br/>。"]
    D7Entry --> D7S1
    D7S2["2. 用户点击 AI 助手，系统打开 AI 工具面板、提示输入框或可用操作列表。"]
    D7S1 --> D7S2
    D7S3["3. 系统把当前选中对象的文本、图片、文件或上下文作为待处理对象显示给用户确认。"]
    D7S2 --> D7S3
    D7S4["4. 用户选择工具、输入提示词或确认处理范围。"]
    D7S3 --> D7S4
    D7S5["5. 用户提交后，系统显示处理中、生成中或 Credit 消耗提示。"]
    D7S4 --> D7S5
    D7S6["6. AI 返回结果后，系统展示预览，并允许用户应用到原对象、创建新对象或取消。"]
    D7S5 --> D7S6
    D7S7["7. 用户确认应用后，系统更新画布并显示新内容选中状态。"]
    D7S6 --> D7S7
    D7S8["8. 如果 AI 失败、Credit 不足或对象类型不支持，系统保留原内容并提示原因。"]
    D7S7 --> D7S8
    D7Alt{"备选：1. 多选时，系统只展示多选适用的锁定、删除、AI、对齐或整理类入口。；2. 当前组件类<br/>型不支持的动作不显示。；3. 对象被锁定时，系统隐藏会改变内容的入口，或要求先解锁。"}
    D7S8 --> D7Alt
    D7Err{"异常：1. 权限不足时，系统不执行修改并提示无权限。；2. 转换、刷新、下载、裁剪或 AI 处<br/>理失败时，系统保留原组件并显示失败反馈。；3. 组件在操作期间被协作者删除或权限改变时，<br/>系统关闭菜单并刷新状态。"}
    D7S8 --> D7Err
    D7Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D7S8 --> D7Perm
  end
  subgraph D8G["删除组件"]
    D8Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D8Entry["可见入口/区域：用户在画布上选中单个组件或框选多个组件后，选中框附近出现悬浮 Widget Menu<br/>。；菜单按对象类型显示有限入口：文本/便利贴可显示字号、字体、字重、对齐、背景色、文<br/>字色、格式应用、切换便利贴类型；连接线可显示颜色、线宽、端点和直线/曲线；图片可显示<br/>裁剪；文件可显示文件名、下载，音频文件可显示转文本。；多数可编辑对象可显示锁定、删除<br/>和 AI 助手；锁定对象通常只保留锁定状态入口。"]
    D8Start --> D8Entry
    D8S1["1. 用户选中一个或多个对象，系统在 Widget Menu 中显示删除入口。"]
    D8Entry --> D8S1
    D8S2["2. 如果对象被锁定或用户无权限，系统隐藏删除入口或显示不可删除状态。"]
    D8S1 --> D8S2
    D8S3["3. 用户点击删除，系统移除允许删除的对象，并关闭或更新 Widget Menu。"]
    D8S2 --> D8S3
    D8S4["4. 删除成功后，对象从画布消失，选区和对齐辅助线同步更新。"]
    D8S3 --> D8S4
    D8S5["5. 多选删除时，系统删除允许对象；遇到锁定或无权限对象时显示部分失败或保留这些对象。"]
    D8S4 --> D8S5
    D8S6["6. 用户可以通过撤销入口恢复刚删除的内容，前提是当前会话支持撤销。"]
    D8S5 --> D8S6
    D8S7["7. 协作中对象被他人删除时，系统刷新本地选区并关闭菜单。"]
    D8S6 --> D8S7
    D8S8["8. 删除失败时，系统保留对象并显示失败反馈。"]
    D8S7 --> D8S8
    D8Alt{"备选：1. 多选时，系统只展示多选适用的锁定、删除、AI、对齐或整理类入口。；2. 当前组件类<br/>型不支持的动作不显示。；3. 对象被锁定时，系统隐藏会改变内容的入口，或要求先解锁。"}
    D8S8 --> D8Alt
    D8Err{"异常：1. 权限不足时，系统不执行修改并提示无权限。；2. 转换、刷新、下载、裁剪或 AI 处<br/>理失败时，系统保留原组件并显示失败反馈。；3. 组件在操作期间被协作者删除或权限改变时，<br/>系统关闭菜单并刷新状态。"}
    D8S8 --> D8Err
    D8Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D8S8 --> D8Perm
  end
```

```mermaid
flowchart TD
  subgraph D9G["刷新组件"]
    D9Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D9Entry["可见入口/区域：用户在画布上选中单个组件或框选多个组件后，选中框附近出现悬浮 Widget Menu<br/>。；菜单按对象类型显示有限入口：文本/便利贴可显示字号、字体、字重、对齐、背景色、文<br/>字色、格式应用、切换便利贴类型；连接线可显示颜色、线宽、端点和直线/曲线；图片可显示<br/>裁剪；文件可显示文件名、下载，音频文件可显示转文本。；多数可编辑对象可显示锁定、删除<br/>和 AI 助手；锁定对象通常只保留锁定状态入口。"]
    D9Start --> D9Entry
    D9S1["1. 用户选中支持刷新的组件，系统在 Widget Menu 中显示刷新入口。"]
    D9Entry --> D9S1
    D9S2["2. 用户点击刷新，系统显示处理中、重新加载中或旋转状态。"]
    D9S1 --> D9S2
    D9S3["3. 系统重新获取或重新渲染该组件内容，原组件保持在画布中。"]
    D9S2 --> D9S3
    D9S4["4. 刷新成功后，组件内容、预览图或状态信息更新，并保持选中状态。"]
    D9S3 --> D9S4
    D9S5["5. 用户可以继续对刷新后的组件进行移动、缩放、锁定或删除。"]
    D9S4 --> D9S5
    D9S6["6. 如果组件类型不支持刷新，系统不显示刷新入口。"]
    D9S5 --> D9S6
    D9S7["7. 如果网络失败、资源失效或权限不足，系统显示刷新失败并保留原内容。"]
    D9S6 --> D9S7
    D9S8["8. 多选状态下，系统只对支持刷新的对象显示批量能力或隐藏刷新入口。"]
    D9S7 --> D9S8
    D9Alt{"备选：1. 多选时，系统只展示多选适用的锁定、删除、AI、对齐或整理类入口。；2. 当前组件类<br/>型不支持的动作不显示。；3. 对象被锁定时，系统隐藏会改变内容的入口，或要求先解锁。"}
    D9S8 --> D9Alt
    D9Err{"异常：1. 权限不足时，系统不执行修改并提示无权限。；2. 转换、刷新、下载、裁剪或 AI 处<br/>理失败时，系统保留原组件并显示失败反馈。；3. 组件在操作期间被协作者删除或权限改变时，<br/>系统关闭菜单并刷新状态。"}
    D9S8 --> D9Err
    D9Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D9S8 --> D9Perm
  end
  subgraph D10G["应用格式"]
    D10Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D10Entry["可见入口/区域：用户在画布上选中单个组件或框选多个组件后，选中框附近出现悬浮 Widget Menu<br/>。；菜单按对象类型显示有限入口：文本/便利贴可显示字号、字体、字重、对齐、背景色、文<br/>字色、格式应用、切换便利贴类型；连接线可显示颜色、线宽、端点和直线/曲线；图片可显示<br/>裁剪；文件可显示文件名、下载，音频文件可显示转文本。；多数可编辑对象可显示锁定、删除<br/>和 AI 助手；锁定对象通常只保留锁定状态入口。"]
    D10Start --> D10Entry
    D10S1["1. 用户选中文本、便利贴或其它支持复制样式的对象，系统显示应用格式入口。"]
    D10Entry --> D10S1
    D10S2["2. 用户点击应用格式，系统进入格式应用状态，并提示用户选择目标对象。"]
    D10S1 --> D10S2
    D10S3["3. 系统把源对象的可复用样式作为当前格式来源，例如字号、字体、字重、文字色、背景色、边<br/>框或对齐方式。"]
    D10S2 --> D10S3
    D10S4["4. 用户点击目标文本、便利贴或兼容对象，系统把适用样式应用到目标对象。"]
    D10S3 --> D10S4
    D10S5["5. 目标对象立即显示新的样式，并保持可见选中或完成反馈。"]
    D10S4 --> D10S5
    D10S6["6. 用户连续点击多个目标时，系统可以继续应用同一格式，直到用户退出该状态。"]
    D10S5 --> D10S6
    D10S7["7. 用户按 Esc、点击选择工具或点击不兼容对象时，系统退出或提示不可应用。"]
    D10S6 --> D10S7
    D10S8["8. 锁定、只读或无权限目标对象不会被应用格式。"]
    D10S7 --> D10S8
    D10Alt{"备选：1. 多选时，系统只展示多选适用的锁定、删除、AI、对齐或整理类入口。；2. 当前组件类<br/>型不支持的动作不显示。；3. 对象被锁定时，系统隐藏会改变内容的入口，或要求先解锁。"}
    D10S8 --> D10Alt
    D10Err{"异常：1. 权限不足时，系统不执行修改并提示无权限。；2. 转换、刷新、下载、裁剪或 AI 处<br/>理失败时，系统保留原组件并显示失败反馈。；3. 组件在操作期间被协作者删除或权限改变时，<br/>系统关闭菜单并刷新状态。"}
    D10S8 --> D10Err
    D10Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D10S8 --> D10Perm
  end
  subgraph D11G["对齐选中组件"]
    D11Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D11Entry["可见入口/区域：用户在画布上选中单个组件或框选多个组件后，选中框附近出现悬浮 Widget Menu<br/>。；菜单按对象类型显示有限入口：文本/便利贴可显示字号、字体、字重、对齐、背景色、文<br/>字色、格式应用、切换便利贴类型；连接线可显示颜色、线宽、端点和直线/曲线；图片可显示<br/>裁剪；文件可显示文件名、下载，音频文件可显示转文本。；多数可编辑对象可显示锁定、删除<br/>和 AI 助手；锁定对象通常只保留锁定状态入口。"]
    D11Start --> D11Entry
    D11S1["1. 用户框选或多选多个对象，系统显示多选边框和对齐/整理入口。"]
    D11Entry --> D11S1
    D11S2["2. 用户打开对齐入口，系统显示左对齐、水平居中、右对齐、顶端对齐、垂直居中、底端对齐、<br/>水平分布和垂直分布等图标按钮。"]
    D11S1 --> D11S2
    D11S3["3. 用户选择对齐或分布动作，系统按当前选区调整对象位置，并保留多选状态。"]
    D11S2 --> D11S3
    D11S4["4. 用户打开整理入口，系统显示可扩展的行列网格；用户悬停时看到当前行列数。"]
    D11S3 --> D11S4
    D11S5["5. 用户点击某个行列组合后，系统按该行列布局重新排列选中对象。"]
    D11S4 --> D11S5
    D11S6["6. 如果选区包含锁定对象，系统只对当前允许移动的对象产生变化，或保持对象位置不变。"]
    D11S5 --> D11S6
    D11S7["7. 用户可以撤销整理动作，系统恢复调整前的位置。"]
    D11S6 --> D11S7
    D11S8["8. 选中对象不足两个时，系统隐藏多对象对齐入口或提示需要多选。"]
    D11S7 --> D11S8
    D11Alt{"备选：1. 多选时，系统只展示多选适用的锁定、删除、AI、对齐或整理类入口。；2. 当前组件类<br/>型不支持的动作不显示。；3. 对象被锁定时，系统隐藏会改变内容的入口，或要求先解锁。"}
    D11S8 --> D11Alt
    D11Err{"异常：1. 权限不足时，系统不执行修改并提示无权限。；2. 转换、刷新、下载、裁剪或 AI 处<br/>理失败时，系统保留原组件并显示失败反馈。；3. 组件在操作期间被协作者删除或权限改变时，<br/>系统关闭菜单并刷新状态。"}
    D11S8 --> D11Err
    D11Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D11S8 --> D11Perm
  end
  subgraph D12G["编辑连接线样式"]
    D12Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D12Entry["可见入口/区域：用户在画布上选中单个组件或框选多个组件后，选中框附近出现悬浮 Widget Menu<br/>。；菜单按对象类型显示有限入口：文本/便利贴可显示字号、字体、字重、对齐、背景色、文<br/>字色、格式应用、切换便利贴类型；连接线可显示颜色、线宽、端点和直线/曲线；图片可显示<br/>裁剪；文件可显示文件名、下载，音频文件可显示转文本。；多数可编辑对象可显示锁定、删除<br/>和 AI 助手；锁定对象通常只保留锁定状态入口。"]
    D12Start --> D12Entry
    D12S1["1. 用户选中连接线，悬浮菜单显示当前连接线可用的颜色、线宽、端点箭头和直线/曲线入口。"]
    D12Entry --> D12S1
    D12S2["2. 用户点击颜色入口，系统展示颜色选项；用户选择颜色后，系统即时更新连接线颜色。"]
    D12S1 --> D12S2
    D12S3["3. 用户点击线宽入口，系统展示可用线宽；用户选择线宽后，系统即时更新连接线粗细。"]
    D12S2 --> D12S3
    D12S4["4. 用户点击端点入口，系统展示起始箭头、尾部箭头、两端箭头或无箭头等可见选项。"]
    D12S3 --> D12S4
    D12S5["5. 用户选择端点样式后，系统更新连接线端点显示，并保留连接对象关系。"]
    D12S4 --> D12S5
    D12S6["6. 用户选择直线或曲线，系统保持连接对象可见，并更新连接线路径显示。"]
    D12S5 --> D12S6
    D12S7["7. 用户点击画布空白处或选择其它对象后，系统关闭菜单并保留最终连接线样式。"]
    D12S6 --> D12S7
    D12S8["8. 锁定连接线被选中时，系统只保留锁定状态入口，不展示会改变样式的入口。"]
    D12S7 --> D12S8
    D12Alt{"备选：1. 多选时，系统只展示多选适用的锁定、删除、AI、对齐或整理类入口。；2. 当前组件类<br/>型不支持的动作不显示。；3. 对象被锁定时，系统隐藏会改变内容的入口，或要求先解锁。"}
    D12S8 --> D12Alt
    D12Err{"异常：1. 权限不足时，系统不执行修改并提示无权限。；2. 转换、刷新、下载、裁剪或 AI 处<br/>理失败时，系统保留原组件并显示失败反馈。；3. 组件在操作期间被协作者删除或权限改变时，<br/>系统关闭菜单并刷新状态。"}
    D12S8 --> D12Err
    D12Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D12S8 --> D12Perm
  end
```

```mermaid
flowchart TD
  subgraph D13G["编辑文本样式"]
    D13Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D13Entry["可见入口/区域：用户在画布上选中单个组件或框选多个组件后，选中框附近出现悬浮 Widget Menu<br/>。；菜单按对象类型显示有限入口：文本/便利贴可显示字号、字体、字重、对齐、背景色、文<br/>字色、格式应用、切换便利贴类型；连接线可显示颜色、线宽、端点和直线/曲线；图片可显示<br/>裁剪；文件可显示文件名、下载，音频文件可显示转文本。；多数可编辑对象可显示锁定、删除<br/>和 AI 助手；锁定对象通常只保留锁定状态入口。"]
    D13Start --> D13Entry
    D13S1["1. 用户选中文本、标题、便利贴或包含文字的形状，系统显示文本样式入口。"]
    D13Entry --> D13S1
    D13S2["2. 用户打开字号入口，系统展示当前可选字号；用户选择后文字大小立即更新。"]
    D13S1 --> D13S2
    D13S3["3. 用户打开字体入口，系统展示当前可用字体；用户选择后文字字体更新。"]
    D13S2 --> D13S3
    D13S4["4. 用户点击加粗、斜体或字重入口，系统更新选中文字的字重表现。"]
    D13S3 --> D13S4
    D13S5["5. 用户选择左对齐、居中、右对齐或其它当前可见对齐方式，系统更新文本排版。"]
    D13S4 --> D13S5
    D13S6["6. 用户选择文字色或背景色，系统即时更新文字或容器颜色。"]
    D13S5 --> D13S6
    D13S7["7. 多选文字对象时，系统只展示共同适用样式，并在值不一致时显示混合状态。"]
    D13S6 --> D13S7
    D13S8["8. 锁定或只读对象不允许修改文字样式。"]
    D13S7 --> D13S8
    D13Alt{"备选：1. 多选时，系统只展示多选适用的锁定、删除、AI、对齐或整理类入口。；2. 当前组件类<br/>型不支持的动作不显示。；3. 对象被锁定时，系统隐藏会改变内容的入口，或要求先解锁。"}
    D13S8 --> D13Alt
    D13Err{"异常：1. 权限不足时，系统不执行修改并提示无权限。；2. 转换、刷新、下载、裁剪或 AI 处<br/>理失败时，系统保留原组件并显示失败反馈。；3. 组件在操作期间被协作者删除或权限改变时，<br/>系统关闭菜单并刷新状态。"}
    D13S8 --> D13Err
    D13Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D13S8 --> D13Perm
  end
  subgraph D14G["将文本转换为便利贴"]
    D14Start["Actor：Board Owner、Board Admin、Board Memb<br/>er"]
    D14Entry["可见入口/区域：用户在画布上选中单个组件或框选多个组件后，选中框附近出现悬浮 Widget Menu<br/>。；菜单按对象类型显示有限入口：文本/便利贴可显示字号、字体、字重、对齐、背景色、文<br/>字色、格式应用、切换便利贴类型；连接线可显示颜色、线宽、端点和直线/曲线；图片可显示<br/>裁剪；文件可显示文件名、下载，音频文件可显示转文本。；多数可编辑对象可显示锁定、删除<br/>和 AI 助手；锁定对象通常只保留锁定状态入口。"]
    D14Start --> D14Entry
    D14S1["1. 用户选中文本框或可拆分文本对象，系统显示文本转便利贴入口。"]
    D14Entry --> D14S1
    D14S2["2. 用户点击入口，系统读取当前文本内容，并判断是否可以按行、段落或列表拆分。"]
    D14S1 --> D14S2
    D14S3["3. 如果可以拆分，系统显示转换中状态，并按拆分结果创建多个便利贴。"]
    D14S2 --> D14S3
    D14S4["4. 新便利贴在画布中按可见布局排列，并显示选中组或多选状态。"]
    D14S3 --> D14S4
    D14S5["5. 系统按当前界面规则保留原文本、替换原文本或提示用户确认处理方式。"]
    D14S4 --> D14S5
    D14S6["6. 用户可以继续移动、改色、编辑或编组生成的便利贴。"]
    D14S5 --> D14S6
    D14S7["7. 如果文本为空、无法拆分或对象类型不支持，系统提示无法转换，不创建便利贴。"]
    D14S6 --> D14S7
    D14S8["8. 如果权限不足或对象锁定，系统隐藏入口或阻止转换。"]
    D14S7 --> D14S8
    D14Alt{"备选：1. 多选时，系统只展示多选适用的锁定、删除、AI、对齐或整理类入口。；2. 当前组件类<br/>型不支持的动作不显示。；3. 对象被锁定时，系统隐藏会改变内容的入口，或要求先解锁。"}
    D14S8 --> D14Alt
    D14Err{"异常：1. 权限不足时，系统不执行修改并提示无权限。；2. 转换、刷新、下载、裁剪或 AI 处<br/>理失败时，系统保留原组件并显示失败反馈。；3. 组件在操作期间被协作者删除或权限改变时，<br/>系统关闭菜单并刷新状态。"}
    D14S8 --> D14Err
    D14Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D14S8 --> D14Perm
  end
```

