# Board 详细交互图

本图按 Board 模块当前 Use Case 的主流程展开，重点表达每个界面能看到什么、能操作什么、操作后出现什么状态。若某个 UC 继续细化，应该同步更新该 UC 的主流程和本图。

## 模块交互展开

该部分保留当前模块已有交互图，用于表达模块内主要页面区域之间的流转。

```mermaid
flowchart TD
  OpenBoard["打开 /board/[boardId]"] --> Load{"是否可访问"}
  Load -->|无权限| Denied["显示无权限、登录、加入或返回入口"]
  Load -->|可访问| Board["显示 Canvas、Header、Board Menu、缩放控件、协作者状态"]
  Board --> Header["Header 操作"]
  Board --> Tool["Board Menu 工具"]
  Board --> Canvas["Canvas 导航和选择"]
  Board --> WidgetMenu["Widget Menu"]
  Board --> Context["Context Menu"]
  Board --> Collaboration["协作状态"]

  Header --> Title["改标题"]
  Title --> TitleSaved["标题更新或失败提示"]
  Header --> Share["分享 Board"]
  Share --> ShareState["权限范围、链接或成员状态更新"]
  Header --> Timer["计时器"]
  Timer --> TimerState["开始、暂停、重置或结束状态"]
  Header --> Slides["Slides"]
  Slides --> SlideState["打开、管理或演示状态"]
  Header --> Export["导出 PDF"]
  Export --> ExportState["生成、下载或失败反馈"]

  Tool --> CreateWidget["创建便利贴、文本、形状、连接线、手绘"]
  CreateWidget --> WidgetSelected["新组件被选中并显示菜单"]
  Tool --> Resources["打开资源或模板面板"]
  Resources --> InsertAsset["插入图片、图标或模板内容"]

  Canvas --> PanZoom["平移、缩放、适配屏幕"]
  Canvas --> Select["选择或多选组件"]
  Select --> WidgetMenu
  Select --> Context
```

## Use Case 主流程展开

```mermaid
flowchart TD
  subgraph D1G["创建 Board"]
    D1Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D1Entry["可见入口/区域：用户在 Room、Team、最近访问或搜索结果中看到白板卡片、搜索框、新建白板按钮、<br/>收藏入口和卡片更多操作菜单。；白板卡片通常展示名称、封面或缩略图、最近编辑信息；无内<br/>容时显示空状态。；用户打开白板后看到加载状态、白板标题、画布内容、Header、Bo<br/>ard Menu、缩放/小地图等可用入口；无权限时看到提示或返回入口。"]
    D1Start --> D1Entry
    D1S1["1. 用户进入 Room、Team 或最近访问页面后，看到白板列表区域、已有白板卡片以及<br/>“创建白板”按钮；在移动端入口显示为加号、新建图标或当前页面提供的创建入口。"]
    D1Entry --> D1S1
    D1S2["2. 用户点击“创建白板”，系统打开“创建白板”弹窗，并把焦点放到“白板名称”输入框。"]
    D1S1 --> D1S2
    D1S3["3. 用户看到“白板名称”字段、默认名称提示、白板标签区域和创建按钮；名称为空时创建按钮<br/>不可用。"]
    D1S2 --> D1S3
    D1S4["4. 用户输入白板名称；如果需要标签，用户在标签输入区域输入标签名并按 Enter，系统<br/>把标签显示成可点击移除的标签块。"]
    D1S3 --> D1S4
    D1S5["5. 用户点击“创建”或在名称输入框中按 Enter，系统将按钮切换为“创建中”或禁用状<br/>态，避免重复提交。"]
    D1S4 --> D1S5
    D1S6["6. 创建成功后，系统关闭弹窗并进入新白板页面；用户看到画布加载完成后的 Header、<br/>白板标题、Board Menu、在线成员和同步状态。"]
    D1S5 --> D1S6
    D1S7["7. 创建失败时，系统停留在弹窗或列表页，恢复按钮可点击状态，并展示失败反馈；用户可以修<br/>改名称后重试或关闭弹窗。"]
    D1S6 --> D1S7
    D1Alt{"备选：1. 用户取消弹窗、关闭菜单或返回上一层时，系统不改变白板数据。；2. 搜索无结果时，系<br/>统展示空状态，并允许用户清空关键词或改用新建入口。；3. 公开访问者或只读用户尝试成员动<br/>作时，系统引导登录、申请加入或提示无权限。"}
    D1S7 --> D1Alt
    D1Err{"异常：1. 权限不足时，系统提示无权限并保留原状态。；2. 网络、保存或加载失败时，系统展示失<br/>败反馈，用户可以重试。；3. 白板已被删除、移动或权限变更时，系统刷新列表或阻止继续使用<br/>旧入口。"}
    D1S7 --> D1Err
    D1Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D1S7 --> D1Perm
  end
  subgraph D2G["打开并协作 Board"]
    D2Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D2Entry["可见入口/区域：用户在 Room、Team、最近访问或搜索结果中看到白板卡片、搜索框、新建白板按钮、<br/>收藏入口和卡片更多操作菜单。；白板卡片通常展示名称、封面或缩略图、最近编辑信息；无内<br/>容时显示空状态。；用户打开白板后看到加载状态、白板标题、画布内容、Header、Bo<br/>ard Menu、缩放/小地图等可用入口；无权限时看到提示或返回入口。"]
    D2Start --> D2Entry
    D2S1["1. 用户在白板列表、最近访问区域、搜索结果或分享链接中看到可访问白板；白板卡片展示名称<br/>、封面或缩略图、最近编辑信息，并在悬停时显示收藏和更多操作入口。"]
    D2Entry --> D2S1
    D2S2["2. 用户点击白板卡片、在更多菜单选择“在新标签页中打开”，或打开他人分享的链接。"]
    D2S1 --> D2S2
    D2S3["3. 系统进入白板加载过程，用户看到页面加载状态；加载完成后，画布内容、左侧 Heade<br/>r 工具、右侧协作工具、在线成员、分享按钮、幻灯片、计时器和同步状态出现在白板上。"]
    D2S2 --> D2S3
    D2S4["4. 如果链接包含视口位置，系统将画布定位到分享时的视图；否则显示白板默认视图。"]
    D2S3 --> D2S4
    D2S5["5. 有编辑权限的用户可以在画布上选择、拖拽、创建或修改内容，并看到同步图标从同步中恢复<br/>为完成状态。"]
    D2S4 --> D2S5
    D2S6["6. 只读或公开访问用户可以查看内容、平移和缩放画布，并看到允许的 Header 入口；<br/>当尝试编辑类入口时，系统隐藏、禁用或提示无权限。"]
    D2S5 --> D2S6
    D2S7["7. 协作者进入或移动时，用户可看到在线成员列表和协作状态变化；画布更新后系统以可见内容<br/>变化和同步状态反馈协作结果。"]
    D2S6 --> D2S7
    D2Alt{"备选：1. 用户取消弹窗、关闭菜单或返回上一层时，系统不改变白板数据。；2. 搜索无结果时，系<br/>统展示空状态，并允许用户清空关键词或改用新建入口。；3. 公开访问者或只读用户尝试成员动<br/>作时，系统引导登录、申请加入或提示无权限。"}
    D2S7 --> D2Alt
    D2Err{"异常：1. 权限不足时，系统提示无权限并保留原状态。；2. 网络、保存或加载失败时，系统展示失<br/>败反馈，用户可以重试。；3. 白板已被删除、移动或权限变更时，系统刷新列表或阻止继续使用<br/>旧入口。"}
    D2S7 --> D2Err
    D2Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D2S7 --> D2Perm
  end
  subgraph D3G["浏览、搜索和打开最近 Board"]
    D3Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D3Entry["可见入口/区域：用户在 Room、Team、最近访问或搜索结果中看到白板卡片、搜索框、新建白板按钮、<br/>收藏入口和卡片更多操作菜单。；白板卡片通常展示名称、封面或缩略图、最近编辑信息；无内<br/>容时显示空状态。；用户打开白板后看到加载状态、白板标题、画布内容、Header、Bo<br/>ard Menu、缩放/小地图等可用入口；无权限时看到提示或返回入口。"]
    D3Start --> D3Entry
    D3S1["1. 用户进入 Room、Team、最近访问或搜索相关页面，看到白板卡片列表、搜索输入框<br/>、创建白板入口；没有白板或没有匹配结果时，系统显示空状态。"]
    D3Entry --> D3S1
    D3S2["2. 用户浏览列表时，每张白板卡片显示名称、封面或缩略图、最近编辑信息；用户悬停卡片时，<br/>系统显示星标收藏按钮和更多操作按钮。"]
    D3S1 --> D3S2
    D3S3["3. 用户在搜索框输入关键词，系统根据关键词刷新白板结果；匹配项保留在列表中，不匹配项被<br/>过滤或搜索结果页不展示。"]
    D3S2 --> D3S3
    D3S4["4. 搜索无结果时，系统显示未找到或空状态提示，并允许用户清空关键词、修改关键词或使用创<br/>建白板入口。"]
    D3S3 --> D3S4
    D3S5["5. 用户点击目标白板卡片，系统打开该白板并显示加载状态。"]
    D3S4 --> D3S5
    D3S6["6. 白板打开成功后，系统进入画布并记录最近访问；用户返回列表后，可以在最近访问区域看到<br/>该白板位置或时间状态更新。"]
    D3S5 --> D3S6
    D3S7["7. 用户打开卡片更多菜单时，可以看到重命名、复制、在新标签页中打开、移动、编辑标签、上<br/>传封面图片、删除等当前可用操作；关闭菜单时列表内容不改变。"]
    D3S6 --> D3S7
    D3Alt{"备选：1. 用户取消弹窗、关闭菜单或返回上一层时，系统不改变白板数据。；2. 搜索无结果时，系<br/>统展示空状态，并允许用户清空关键词或改用新建入口。；3. 公开访问者或只读用户尝试成员动<br/>作时，系统引导登录、申请加入或提示无权限。"}
    D3S7 --> D3Alt
    D3Err{"异常：1. 权限不足时，系统提示无权限并保留原状态。；2. 网络、保存或加载失败时，系统展示失<br/>败反馈，用户可以重试。；3. 白板已被删除、移动或权限变更时，系统刷新列表或阻止继续使用<br/>旧入口。"}
    D3S7 --> D3Err
    D3Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D3S7 --> D3Perm
  end
  subgraph D4G["收藏或取消收藏 Board"]
    D4Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D4Entry["可见入口/区域：用户在 Room、Team、最近访问或搜索结果中看到白板卡片、搜索框、新建白板按钮、<br/>收藏入口和卡片更多操作菜单。；白板卡片通常展示名称、封面或缩略图、最近编辑信息；无内<br/>容时显示空状态。；用户打开白板后看到加载状态、白板标题、画布内容、Header、Bo<br/>ard Menu、缩放/小地图等可用入口；无权限时看到提示或返回入口。"]
    D4Start --> D4Entry
    D4S1["1. 用户在白板列表或最近访问区域看到白板卡片；当卡片已收藏时，星标常亮显示，未收藏时星<br/>标可在悬停卡片后出现。"]
    D4Entry --> D4S1
    D4S2["2. 用户点击星标按钮，系统阻止卡片被误打开，并开始切换收藏状态。"]
    D4S1 --> D4S2
    D4S3["3. 收藏成功时，星标变为高亮填充状态；取消收藏成功时，星标恢复为未收藏状态或在鼠标移出<br/>后隐藏。"]
    D4S2 --> D4S3
    D4S4["4. 如果当前页面包含收藏分组或收藏列表，系统同步刷新该白板在收藏区域中的出现或移除状态<br/>。"]
    D4S3 --> D4S4
    D4S5["5. 用户可以再次点击星标切换回原状态，系统继续用星标颜色和列表刷新反馈结果。"]
    D4S4 --> D4S5
    D4S6["6. 操作失败时，系统保留原收藏状态，并显示失败反馈；用户仍停留在当前列表。"]
    D4S5 --> D4S6
    D4S7["7. 公开访问者或无权限用户看不到收藏入口，或点击后被引导登录、加入空间或收到无权限提示<br/>。"]
    D4S6 --> D4S7
    D4Alt{"备选：1. 用户取消弹窗、关闭菜单或返回上一层时，系统不改变白板数据。；2. 搜索无结果时，系<br/>统展示空状态，并允许用户清空关键词或改用新建入口。；3. 公开访问者或只读用户尝试成员动<br/>作时，系统引导登录、申请加入或提示无权限。"}
    D4S7 --> D4Alt
    D4Err{"异常：1. 权限不足时，系统提示无权限并保留原状态。；2. 网络、保存或加载失败时，系统展示失<br/>败反馈，用户可以重试。；3. 白板已被删除、移动或权限变更时，系统刷新列表或阻止继续使用<br/>旧入口。"}
    D4S7 --> D4Err
    D4Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D4S7 --> D4Perm
  end
```

```mermaid
flowchart TD
  subgraph D5G["更新 Board 元信息"]
    D5Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D5Entry["可见入口/区域：用户在 Room、Team、最近访问或搜索结果中看到白板卡片、搜索框、新建白板按钮、<br/>收藏入口和卡片更多操作菜单。；白板卡片通常展示名称、封面或缩略图、最近编辑信息；无内<br/>容时显示空状态。；用户打开白板后看到加载状态、白板标题、画布内容、Header、Bo<br/>ard Menu、缩放/小地图等可用入口；无权限时看到提示或返回入口。"]
    D5Start --> D5Entry
    D5S1["1. 用户在白板卡片上悬停并点击更多操作按钮，看到重命名、编辑标签、上传封面图片、移除封<br/>面图片等当前可用菜单项；进入白板后也可以在 Header 标题输入框直接修改白板名称。"]
    D5Entry --> D5S1
    D5S2["2. 用户选择“重命名”或点击 Header 标题，系统显示当前白板名称；在弹窗中显示“<br/>新白板名称”输入框，在 Header 中显示可编辑标题输入状态。"]
    D5S1 --> D5S2
    D5S3["3. 用户输入新名称并点击“确认”、按 Enter 或让标题输入框失焦；系统在保存期间禁<br/>用输入或显示处理中状态。"]
    D5S2 --> D5S3
    D5S4["4. 保存成功后，Header 标题、浏览器标题和列表卡片名称更新；保存失败时，系统恢复<br/>原名称并提示失败。"]
    D5S3 --> D5S4
    D5S5["5. 用户选择“编辑标签”时，系统打开标签编辑弹窗；用户添加或移除标签后关闭或保存，卡片<br/>标签信息随之更新。"]
    D5S4 --> D5S5
    D5S6["6. 用户选择“上传封面图片”时，系统打开文件选择器；用户选择图片后，系统上传并更新卡片<br/>封面，成功时显示“封面图片上传成功”，失败时显示失败提示。"]
    D5S5 --> D5S6
    D5S7["7. 当白板已有封面且“移除封面图片”入口显示时，用户点击后系统移除封面并刷新卡片；失败<br/>时保留原封面并提示。"]
    D5S6 --> D5S7
    D5Alt{"备选：1. 用户取消弹窗、关闭菜单或返回上一层时，系统不改变白板数据。；2. 搜索无结果时，系<br/>统展示空状态，并允许用户清空关键词或改用新建入口。；3. 公开访问者或只读用户尝试成员动<br/>作时，系统引导登录、申请加入或提示无权限。"}
    D5S7 --> D5Alt
    D5Err{"异常：1. 权限不足时，系统提示无权限并保留原状态。；2. 网络、保存或加载失败时，系统展示失<br/>败反馈，用户可以重试。；3. 白板已被删除、移动或权限变更时，系统刷新列表或阻止继续使用<br/>旧入口。"}
    D5S7 --> D5Err
    D5Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D5S7 --> D5Perm
  end
  subgraph D6G["移动 Board 到其他空间"]
    D6Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D6Entry["可见入口/区域：用户在 Room、Team、最近访问或搜索结果中看到白板卡片、搜索框、新建白板按钮、<br/>收藏入口和卡片更多操作菜单。；白板卡片通常展示名称、封面或缩略图、最近编辑信息；无内<br/>容时显示空状态。；用户打开白板后看到加载状态、白板标题、画布内容、Header、Bo<br/>ard Menu、缩放/小地图等可用入口；无权限时看到提示或返回入口。"]
    D6Start --> D6Entry
    D6S1["1. 用户在白板卡片上悬停，点击更多操作按钮，并选择“移动到另一个房间”。"]
    D6Entry --> D6S1
    D6S2["2. 系统关闭下拉菜单并打开“移动白板”弹窗，展示“选择要移动到的房间”的下拉选择器。"]
    D6S1 --> D6S2
    D6S3["3. 用户打开房间选择器，看到当前团队中可选择的房间；当前白板所在房间不会作为可移动目标<br/>展示。"]
    D6S2 --> D6S3
    D6S4["4. 用户选择目标房间后，“移动”按钮变为可点击；未选择目标房间时按钮保持禁用。"]
    D6S3 --> D6S4
    D6S5["5. 用户点击“移动”，系统显示移动中或禁用按钮，并关闭或保持弹窗等待操作完成。"]
    D6S4 --> D6S5
    D6S6["6. 移动成功后，当前房间列表中该白板消失，目标房间列表中出现该白板；用户在最近访问或搜<br/>索结果中看到所属位置变化。"]
    D6S5 --> D6S6
    D6S7["7. 移动失败或目标房间权限不足时，系统保留白板在原房间，并显示失败或无权限反馈，用户可<br/>以重新选择目标或取消。"]
    D6S6 --> D6S7
    D6Alt{"备选：1. 用户取消弹窗、关闭菜单或返回上一层时，系统不改变白板数据。；2. 搜索无结果时，系<br/>统展示空状态，并允许用户清空关键词或改用新建入口。；3. 公开访问者或只读用户尝试成员动<br/>作时，系统引导登录、申请加入或提示无权限。"}
    D6S7 --> D6Alt
    D6Err{"异常：1. 权限不足时，系统提示无权限并保留原状态。；2. 网络、保存或加载失败时，系统展示失<br/>败反馈，用户可以重试。；3. 白板已被删除、移动或权限变更时，系统刷新列表或阻止继续使用<br/>旧入口。"}
    D6S7 --> D6Err
    D6Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D6S7 --> D6Perm
  end
  subgraph D7G["复制 Board"]
    D7Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D7Entry["可见入口/区域：用户在 Room、Team、最近访问或搜索结果中看到白板卡片、搜索框、新建白板按钮、<br/>收藏入口和卡片更多操作菜单。；白板卡片通常展示名称、封面或缩略图、最近编辑信息；无内<br/>容时显示空状态。；用户打开白板后看到加载状态、白板标题、画布内容、Header、Bo<br/>ard Menu、缩放/小地图等可用入口；无权限时看到提示或返回入口。"]
    D7Start --> D7Entry
    D7S1["1. 用户在白板卡片上悬停，点击更多操作按钮，并在入口显示时选择“复制”。"]
    D7Entry --> D7S1
    D7S2["2. 系统关闭菜单并显示“正在复制白板...”之类的处理中反馈，避免用户误以为没有响应。"]
    D7S1 --> D7S2
    D7S3["3. 复制处理中，原白板卡片仍保留在列表中；用户可以继续浏览，但重复点击同一复制入口不应<br/>产生混乱反馈。"]
    D7S2 --> D7S3
    D7S4["4. 复制成功后，系统刷新当前列表、最近访问或搜索结果；用户看到新增的副本白板卡片。"]
    D7S3 --> D7S4
    D7S5["5. 副本卡片展示复制后的白板名称、封面或缩略图、最近编辑信息；用户可以像打开普通白板一<br/>样点击进入副本。"]
    D7S4 --> D7S5
    D7S6["6. 如果用户从模板类列表查看白板，且复制入口不显示，用户只能使用当前页面提供的其他可用<br/>入口。"]
    D7S5 --> D7S6
    D7S7["7. 复制失败时，系统显示失败反馈，不新增副本卡片，原白板状态保持不变。"]
    D7S6 --> D7S7
    D7Alt{"备选：1. 用户取消弹窗、关闭菜单或返回上一层时，系统不改变白板数据。；2. 搜索无结果时，系<br/>统展示空状态，并允许用户清空关键词或改用新建入口。；3. 公开访问者或只读用户尝试成员动<br/>作时，系统引导登录、申请加入或提示无权限。"}
    D7S7 --> D7Alt
    D7Err{"异常：1. 权限不足时，系统提示无权限并保留原状态。；2. 网络、保存或加载失败时，系统展示失<br/>败反馈，用户可以重试。；3. 白板已被删除、移动或权限变更时，系统刷新列表或阻止继续使用<br/>旧入口。"}
    D7S7 --> D7Err
    D7Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D7S7 --> D7Perm
  end
  subgraph D8G["删除 Board"]
    D8Start["Actor：Board Owner、Board Admin、Board Memb<br/>er、Board Visitor、public visitor"]
    D8Entry["可见入口/区域：用户在 Room、Team、最近访问或搜索结果中看到白板卡片、搜索框、新建白板按钮、<br/>收藏入口和卡片更多操作菜单。；白板卡片通常展示名称、封面或缩略图、最近编辑信息；无内<br/>容时显示空状态。；用户打开白板后看到加载状态、白板标题、画布内容、Header、Bo<br/>ard Menu、缩放/小地图等可用入口；无权限时看到提示或返回入口。"]
    D8Start --> D8Entry
    D8S1["1. 用户在白板卡片上悬停，点击更多操作按钮，并选择“删除”。"]
    D8Entry --> D8S1
    D8S2["2. 系统关闭下拉菜单并打开“删除白板”确认弹窗，弹窗中显示要删除的白板名称以及删除确认<br/>文案。"]
    D8S1 --> D8S2
    D8S3["3. 用户可以点击“取消”关闭弹窗，系统不删除白板，卡片仍保留在原列表。"]
    D8S2 --> D8S3
    D8S4["4. 用户点击“删除”确认，系统将删除按钮切换为删除中状态，并显示加载图标或禁用重复操作<br/>。"]
    D8S3 --> D8S4
    D8S5["5. 删除成功后，确认弹窗关闭，白板卡片从当前列表、最近访问或搜索结果中移除。"]
    D8S4 --> D8S5
    D8S6["6. 如果删除后列表为空，系统显示空状态，并保留创建白板或返回上级的可用入口。"]
    D8S5 --> D8S6
    D8S7["7. 删除失败或权限不足时，系统关闭或保留弹窗并显示失败反馈；白板卡片仍显示在列表中，用<br/>户可以稍后重试。"]
    D8S6 --> D8S7
    D8Alt{"备选：1. 用户取消弹窗、关闭菜单或返回上一层时，系统不改变白板数据。；2. 搜索无结果时，系<br/>统展示空状态，并允许用户清空关键词或改用新建入口。；3. 公开访问者或只读用户尝试成员动<br/>作时，系统引导登录、申请加入或提示无权限。"}
    D8S7 --> D8Alt
    D8Err{"异常：1. 权限不足时，系统提示无权限并保留原状态。；2. 网络、保存或加载失败时，系统展示失<br/>败反馈，用户可以重试。；3. 白板已被删除、移动或权限变更时，系统刷新列表或阻止继续使用<br/>旧入口。"}
    D8S7 --> D8Err
    D8Perm["权限/可见性：1. Board Owner：可以进入 Board、编辑内容、管理标题和内容生命周期，并<br/>可在当前 Board、Room、Team 权限允许范围内管理分享范围、删除、移动、复制等<br/>高风险操作。；2. Board Admin：可以编辑内容并使用多数协作入口；涉及可见范围<br/>、删除、移动、备份恢复等管理动作时，需要具备对应 Room/Team/Board 管理权<br/>限。；3. Board Member：可以在有编辑权限的 Board 中创建、修改、移动<br/>、复制和删除自己可操作的内容；不默认拥有权限策略和 Board 生命周期管理能力。；4.<br/> Board Visitor：可以查看允许访问的内容，使用平移、缩放、查看同步状态和在线<br/>状态等查看能力；不能创建、编辑、删除或改变分享范围。；5. Public visitor<br/>：通过公开链接进入时只能获得公开策略允许的查看能力；如需要保存、编辑、加入协作或访问非公<br/>开 Board，系统应引导登录、加入空间或提示无权限。"]
    D8S7 --> D8Perm
  end
```

