# AGENTS.md — Phase p7 (Board 壳 (P7)) 局部指令

> 阶段级 scoped 指令,补充根 AGENTS.md。只写本阶段特有的约束。

## 本阶段焦点
Board UI 壳：header(标题/分享/计时器/导出…) + board-menu(各组件创建入口) + context-menu(层级/分组/锁定) + local-workspace

## 权威来源
- 功能清单:本目录 `feature_list.json`(本阶段唯一权威)。
- 进度:本目录 `progress.md`。

## 规则继承
根 `AGENTS.md` 的所有硬约束在此继续生效(尤其"完成定义"与"干净收尾")。
