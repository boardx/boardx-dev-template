# Sprint p7/01 — Board 壳干净子集：设置(F05)/计时器(F04)/帮助引导(F07)。统计/备份依赖 board-keyed items blocked-on p6

- **所属阶段**: Phase p7 (board-shell)
- **创建于**: 2026-06-29 20:17:49

## 本 sprint 目标
Board 壳干净子集：设置(F05)/计时器(F04)/帮助引导(F07)。统计/备份依赖 board-keyed items blocked-on p6

## 领取的 feature(引用自阶段权威清单,按 id)
- F04 (P4, board-header) — 协作计时器
- F05 (P5, board-header) — Board 设置与交互偏好
- F07 (P7, board-header) — 快捷键帮助 + 欢迎引导

> 实际工作集见同目录 `active-features.json`(脚本派生,只读,勿手改)。
> 修改功能归属:改阶段 `feature_list.json` 里对应 feature 的 `sprint` 字段,再重跑
> `pnpm harness new-sprint`(或 refresh)重新派生。

## 完成标准
- 上述每个 feature 经 `pnpm harness verify --sprint p7/01` 门控为 `passing`。
- `session-handoff.md` 与 `progress.md` 已更新。
