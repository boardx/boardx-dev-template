# Sprint p6/03 — board-keyed items 地基(ADR-0002) + 选择/多选/键盘(F06)

- **所属阶段**: Phase p6 (canvas)
- **创建于**: 2026-06-30 00:29:13

## 本 sprint 目标
board-keyed items 地基(ADR-0002) + 选择/多选/键盘(F06)

## 领取的 feature(引用自阶段权威清单,按 id)
- F06 (P6, canvas) — 选择 / 多选组件 + 键盘操作

> 实际工作集见同目录 `active-features.json`(脚本派生,只读,勿手改)。
> 修改功能归属:改阶段 `feature_list.json` 里对应 feature 的 `sprint` 字段,再重跑
> `pnpm harness new-sprint`(或 refresh)重新派生。

## 完成标准
- 上述每个 feature 经 `pnpm harness verify --sprint p6/03` 门控为 `passing`。
- `session-handoff.md` 与 `progress.md` 已更新。
