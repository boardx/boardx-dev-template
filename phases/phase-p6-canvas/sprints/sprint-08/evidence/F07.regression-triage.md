# F07 回归 triage（2026-07-03，第二轮返工）

## 背景
merge origin/main（带 #313 evidence gitignore 白名单 + p17-F01 底部 dock/AI 浮层）后，
对 canvas/widget/board 相关 43 个 e2e spec 文件做了一轮顺序回归（单 worker，逐文件跑，
未并发），用于比对 `evidence/F13.verify-full-triage.md` 已归档的预置红灯。

**F07 本体验证（阻断项 §2 的 3 条 verification）已单独跑绿**，见同目录
`evidence/F07.verify.log`（docker up / migrate / canvas-guidelines.spec.ts 3/3 passed）。
本文档是在此基础上做的更大范围回归扫描，供交叉核对，非 F07 verification 的一部分。

## 运行方式与已知局限
```bash
cd apps/web
for f in <43 个 spec>; do pnpm exec playwright test "$f" --reporter=list; done
```
- 逐文件单独调用 playwright（而非一次性传全部文件名——大数组参数在本机 playwright 1.47
  CLI 下会触发 "No tests found" 的已知问题，超过约 5 个文件名参数即复现，与本次改动
  无关，纯 CLI 调用方式问题，已绕过）。
- **未做隔离**：43 个文件共享同一个 dev server + 数据库进程，顺序执行、无重启，因此不能
  排除"数据库状态被前序 spec 污染 / dev server 长跑后资源争用"造成的假阳性。下面第 3
  组失败（canvas-add/delete/render/update）单独重跑 canvas-render.spec.ts 时因回归任务
  被提前终止导致 dev server 重启中，未能拿到干净的单跑结果确认，**留待下一轮验证**。
- 因 coordinator 要求"不等完整回归"立即推送，本次回归在跑完 43/43 文件后（最后一个
  widgets-004-shape-widget.spec.ts 被手动中止）即收尾，未做失败项的逐一隔离重跑。

## 结果汇总
- 总文件数：43（42 个跑完得到明确 pass/fail，1 个 widgets-004 被中止未计入）
- 文件级别失败：10 个（部分文件内是多测试用例，仅其中 1 条失败，其余同文件用例通过）

## 失败清单与归因

| Spec 文件 | 失败用例 | 归因 |
|---|---|---|
| board-list-search.spec.ts | UI：搜索过滤房间白板列表 | **新（不在 F13 triage 内）**——board 列表搜索 UI，与 F07 渲染层无关，疑似列表刷新时序 flake（`not.toContainText` 超时但看到的文本恰是应过滤掉的旧值滞留）。同文件另一条「无匹配显示空提示」（F13 triage 已收录）通过 |
| board-menu-001-use-board-menu.spec.ts | 编辑者使用 Board Menu：工具可见、面板可打开、创建后选中 | **同名收录于 F13.verify-full-triage.md 第 12 行**（"编辑者使用 Board Menu：工具可见、面板可打开、创建后选中"），预置红灯，非本轮引入 |
| board-menu-003-create-text.spec.ts | 添加文本 → 画布出现文本组件并自动选中 | **新，但与 F13 triage 第 30 行「添加形状（rect）→ 出现在板上、自动选中、刷新仍在」同一失败类别**：toolbar 按钮创建 item 后 `selection-count` 未变为 1（自动选中未生效），只是这次命中的是文本创建路径而非形状创建路径。根因待查，但症状与已归档预置红灯一致，判定为同族问题 |
| board-ai-overlay.spec.ts | 从底部 dock 新建便签：dock 与画布内容真值一致 | **新（p17-F01 测试，F13 triage 编写时该 spec 尚不存在）**：断言 `items-layer` 内 `[data-testid^="item-"]` 计数，但 F13 把渲染层切到 fabric.Canvas 后，item 本体画在 `<canvas>` 上，不再产出该 DOM testid 节点——这是 **p17-F01 测试与 F13 渲染层重构之间的真实断层**，不是 flake。**需要后续修复**：p17-F01 的 e2e 断言需要改用 fabric 侧的计数方式（比照 `e2e/helpers/canvas.ts` 里 F13 已有的 `expectItemCount`/`canvasItems` 辅助），或者 fabric-canvas.tsx 需要为已渲染 item 补一层可查询的无障碍/测试锚点。本轮未动 dock/overlay 代码（范围纪律），仅记录 |
| board-visibility.spec.ts | UI：房间 owner 在板页切换可见范围到 public | **未在 F13 triage 内**，未及单独复测确认是否 flake，标记为待确认 |
| canvas-add.spec.ts | 添加便签后出现在板上且刷新仍在 | 同 board-ai-overlay 症状：`items-layer [data-testid^="item-"]` count 0。但 **F07.verify.log 里 canvas-guidelines.spec.ts（同样依赖 fabric 渲染 + items-layer 断言）刚刚独立跑通过**，且此 spec 在 43 文件顺序跑的靠后位置（第 24/43），怀疑是长跑后 dev server/数据库状态问题而非结构性缺陷；单独重跑 canvas-render.spec.ts 验证时因本次回归被 coordinator 要求的提前终止打断（dev server 重启中，遇到 `Timed out 120000ms from config.webServer`），**未拿到确凿的单跑结果**，需要下一轮单独重跑确认 |
| canvas-delete.spec.ts | 删除 item：板上消失且列表不含 | 同上，疑似同一批 dev-server/状态 flake，待单独复测确认 |
| canvas-render.spec.ts | 打开白板画布，渲染已有 item | 同上，疑似同一批 dev-server/状态 flake，待单独复测确认 |
| canvas-update.spec.ts | 移动 + 编辑 item 并持久化 | 同上，疑似同一批 dev-server/状态 flake，待单独复测确认 |
| widgets-001-use-canvasx-widgets.spec.ts | 编辑者创建并管理 CanvasX widgets，菜单显示能力边界 | **同名收录于 F13.verify-full-triage.md 第 29 行**，预置红灯，非本轮引入 |

## 结论（诚实小结，非最终定论）
- **明确判定为预置红灯（同 F13 triage 同名）**：board-menu-001、widgets-001 —— 2 例。
- **同族已知问题（症状与 triage 已收录问题一致，仅命中路径不同）**：board-menu-003 —— 1 例。
- **新发现、且已判定为真实断层（非 flake）**：board-ai-overlay 的 items-layer DOM 断言
  与 F13 fabric 渲染层不兼容 —— 1 例。这是 p17-F01 测试代码与 F13 渲染重构之间的接口
  断层，按范围纪律本轮不改 dock/overlay 代码，**留给 p17-F01 owner 或下一轮修复**。
- **未决（怀疑是长跑批量执行的环境 flake，需要下一轮单独复测确认）**：
  board-list-search、board-visibility、canvas-add、canvas-delete、canvas-render、
  canvas-update —— 6 例。**不能排除是真实回归**，如实标注为未决，不做无证据的"这是
  flake"断言。

## 后续行动项（下一轮 session 需要做）
1. 逐一单独重跑上述 6 例「未决」spec（干净 dev server + 干净数据库，不与其它 spec 顺序共享
   进程），确认是 flake 还是真实回归；如是真实回归，定位并修复。
2. board-ai-overlay.spec.ts 的 items-layer 断言需要 p17-F01 owner 或本 worker 下一轮
   配合修复，改用 fabric 侧的 item 计数辅助（`e2e/helpers/canvas.ts`）。
3. board-menu-003 的「创建后自动选中」失效与 F13 triage 里「添加形状自动选中」失效应合并
   排查，可能是同一处 `selectItem`/自动选中调用路径的通用缺陷。

## 复现命令
```bash
cd apps/web
pnpm exec playwright test e2e/canvas-guidelines.spec.ts   # F07 本体，绿，见 F07.verify.log
pnpm exec playwright test e2e/board-menu-001-use-board-menu.spec.ts   # 预置红灯，见上表
pnpm exec playwright test e2e/board-ai-overlay.spec.ts    # 新断层，见上表
```
