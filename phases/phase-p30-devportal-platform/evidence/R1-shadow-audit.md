# R1 影子模式对照台账（模板，p30-F10）

> **本文件是模板，不含真实审计数据。** CoordBrain 影子模式部署后需要真实运行满一个
> 影子周期（G5 已拍板：≥24h 且 ≥1 完整 C-cycle，取长者——当前 CYCLE_HOURS=3h，故
> 门槛恒为 24h）才有审计对象；`verify-shadow-cycle.sh` 会检查事件流跨度是否达标。
> **不允许自评零误判**——本台账必须由人类逐条核对 CoordBrain 的影子决策与实际发生
> 的事实是否一致，核对人签字后才能作为 R1 通关证据（渐进接管保序：R2 开工前必须
> 引用一份填写完毕、误判计数为 0 的本文件副本）。

## 核对范围

- 周期起止：`____________`（取自 `GET /api/coord/repos/<owner>/<repo>/shadow-cycle-status` 的 `first_at` ~ `last_at`）
- 覆盖仓：`____________`
- 核对人：`____________`（人类，非 agent 自证）
- 核对时间：`____________`

## 五类规则逐条核对

对每一类规则，从 `GET /api/coord/repos/<owner>/<repo>/shadow-decisions` 抽样（建议：
全部 `decision=true` 的条目 + 抽样若干 `decision=false` 的边界条目），逐条核对
CoordBrain 的判定是否与人工独立判断一致。

| 规则 | 抽样条数 | 命中正确数 | 误判数 | 备注 |
|---|---|---|---|---|
| merge_ready（全绿可合并） | | | **0** | andon 激活期间的样本单独核对（边界） |
| dispatch_suggested（ready-for-dev 派工） | | | **0** | |
| pr_nudge（PR 超时催办） | | | **0** | |
| stale_lease_reclaim（stale 租约回收） | | | **0** | |
| andon_freeze（andon 冻结） | | | **0** | |

## andon 边界专项核对（渐进接管红线）

> 这是 R1→R5 全程不可违反的安全约束：andon 激活时，无论 PR 判定多"绿"，
> merge_ready 决策必须为 false。

- [ ] 周期内是否发生过至少一次 andon 激活/清除？（若周期内始终未激活，此项标注
      「周期内无 andon 样本」，不算通过，需在下一周期补充覆盖或用手动测试触发验证）
- [ ] 若发生过：激活期间所有 open PR 的 `merge_ready` 决策是否全部为 `false`，
      `reason` 是否为 `andon_active`？
- [ ] 若发生过：`andon_freeze` 决策是否与 andon 实际激活状态逐次对齐？

## 误判总计

**误判总数：____（必须为 0 才能作为 R2 开工前置证据）**

如有误判，逐条记录：

| 决策 event_id | 规则 | CoordBrain 判定 | 实际应有判定 | 根因 |
|---|---|---|---|---|
| | | | | |

## 结论

- [ ] 零误判，本台账可作为 R2（机械合并接管）开工前置证据
- [ ] 存在误判，本轮 R1 未通关，需先修正规则/重新走满一个周期

核对人签字：____________________  日期：____________________
