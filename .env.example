# .env.example — 复制为 .env 后按需改。骨架默认值与 infra/docker-compose.yml 对齐。
DATABASE_URL=postgresql://boardx:boardx@localhost:5432/boardx
REDIS_URL=redis://localhost:6379

# web 调用 worker/data 时用到；默认本地。
WEB_PORT=3000
